# P0—P1 执行与回归记录

日期：2026-07-28  
本次复测：2026-07-30  
适用源码：`.winpress-media-integration-20260726`  
验收依据：[`PRODUCT-PRD-V4.2.md`](PRODUCT-PRD-V4.2.md) 与 [`WINPRESS-GLOBAL-PRODUCT-CODE-AUDIT-20260728.md`](WINPRESS-GLOBAL-PRODUCT-CODE-AUDIT-20260728.md)  
本机演示环境：首页 `http://127.0.0.1:5217/`；后端健康检查 `http://127.0.0.1:8192/api/v1/health`

## 验收边界

本记录只确认当前源码与本机演示环境中的可重复结果。它不构成外部媒体数据、供应商履约、法律主体、案例授权、生产域名或生产凭据已验收的声明。

- 可独立下单的服务仅为：现场采写（`ONSITE_WRITING`）、媒体邀请（`MEDIA_PR`）、直编发稿（`DIRECT_PUBLISHING`）和新闻发布会（`NEWS_CONFERENCE`）。
- API 接入为商务与技术评估，不是第五种日常下单服务。
- 云采写/现场采写不等同于媒体采访、报道承诺或媒体邀请；活动关联只用于项目归组，不合并服务价格、订单或任务。
- 客户投影不得返回供应商、成本、上游媒体/记者原始标识、存储键、令牌、运营备注或内部规则。

## 本轮 P0/P1 修复

| 优先级 | 修复 | 当前可核验行为 |
|---|---|---|
| P0 | 附件访问边界 | 上传需关联项目；下载使用受认证的 `fileNo` 接口并复核项目范围；静态 `/files/` 已禁用。 |
| P0 | 客户数据投影 | 项目、工作项、任务记录、订单台账、发布计划、媒体候选与接口错误均做客户字段白名单；客户页面不显示成本、供应商、上游标识或令牌。 |
| P0 | 渠道目录投影 | `/channels` 不返回平台渠道编号或报价主键；目录和客户服务价在页面明确为待项目核验的方案参考，不表述为实时媒体库存或发布承诺。 |
| P0 | 权限隔离 | 客户不能读取写手派单；未获分配的运营不能读取客户项目；运营不能枚举客户需求。 |
| P0 | 后台计价与供应商隔离 | `/admin/pricing`、报价汇总、供应商及供应商订单接口仅允许 `PLATFORM_ADMIN`；客户与发布运营均返回拒绝，后台管理员可在受控后台维护。 |
| P0 | 客户账号与租户隔离 | 本机三类快速登录账号与四个品牌案例客户均按文档密码验证；品牌案例表的项目说明不再被误作密码。第二个客户读取首个客户项目返回拒绝。 |
| P0 | 外部媒体边界 | 未配置正式授权时媒体发现显示不可用并提供人工补充，不伪造实时媒体或记者结果；上游返回的运行提示不透传，对客候选仅说明“待项目核验”。 |
| P0 | 运行时结构就绪 | 健康接口不再仅回传源码常量；它会核验当前数据库满足四服务闭环所需的关键表与字段。结构未就绪时返回通用升级状态，不暴露迁移细节。 |
| P1 | 四服务受理任务 | 媒体邀请与直编发稿补齐受理任务；四类服务均可形成项目、任务与订单记录。 |
| P1 | 活动关联 | 新闻发布会可作为活动根项目关联其他独立服务项目；各项目仍分别计价、下单和执行。 |
| P1 | 稿件复用 | 直编发稿可选择客户已确认稿件，服务端只复制已审批版本到新项目；原稿不被改写。 |
| P1 | 稿件来源可追溯 | 客户项目页保留来源项目与来源稿件的可读标签；来源内部 ID、供应商、成本和上游参考仍不返回。 |
| P1 | 移动端导航 | 操作台移动端只保留一套顶部频道导航；操作菜单可打开、关闭按钮可关闭、点击右侧遮罩亦可关闭。 |

## 2026-07-28 实际回归证据

| 检查 | 结果 |
|---|---|
| Vue 类型检查与生产构建 | `npm.cmd run build` 通过。 |
| Java 自动化测试 | 8 个报告文件、55 项测试通过；失败 0、错误 0、跳过 0。 |
| 本机 Docker 编排 | `docker-compose.local-demo.yml config --quiet` 通过；前端、后端、PostgreSQL、Redis 均为 `healthy`。 |
| 生产编排静态校验 | 使用 `.env.example` 作为字段完整性输入执行 `docker-compose.production.yml config --quiet` 通过；未启动生产容器。 |
| 健康接口 | 后端、数据库与 `schemaStatus` 均为 `UP`；`schemaVersion=20` 仅在当前数据库通过关键结构校验后才返回。 |
| 数据库实查 | `service_intake_task` 表、活动根项目字段及约束、稿件来源字段及约束均实际存在；缺失任一关键结构时健康接口会返回通用的升级状态。 |
| 接口与权限回归 | `scripts/verify-local-p0p1.ps1` 通过：匿名附件拒绝、公开静态文件拒绝、公开测试账号拒绝、内部 GEO 状态不公开、三类角色会话、客户范围、运营越权拒绝、媒体状态字段白名单、渠道目录不含内部渠道号/报价主键、稿件来源选择、项目/订单字段边界均已检查。 |
| 四服务闭环 | 2026-07-29 使用 `-CreateActivityClosure` 在本机隔离演示库新建发布会根项目及现场采写、媒体邀请、直编发稿三个独立子项目；四类服务均生成各自的项目、任务与订单记录，直编发稿验证了已确认稿件复制。该记录仅存在于本机演示库。 |
| 桌面与平板页面 | 首页在 1440×900、768×900 下无横向溢出、无缺失图片；登录后的直编发稿页在 1440×900 下可见“项目核验”边界提示，未显示成本、供应商、上游、令牌、内部渠道号或报价主键字段。 |
| 移动端页面 | 首页、新闻发布会下单页与登录后的直编发稿页在 390×844 下无横向溢出；首页移动导航可展开并关闭，完整频道项可见。直编发稿页保留非实时库存、待项目核验说明且无内部字段。 |

## 2026-07-29 P0 加固与复测

| 检查 | 结果 |
|---|---|
| 客户字段投影 | `work-items`、`task-records`、`order-records` 与项目下 `publish-plans` 改为正向字段清单。回归数据故意加入供应商、成本、上游参考、操作人和内部备注字段，客户响应均未返回。 |
| 客户项目与任务投影 | `projects`、`publish-tasks` 和项目详情中的项目、发布会清单、候选与任务也改为正向字段清单。客户不再收到负责人、内部预算、稿件内部标识、执行备注、异常原因、候选短时引用、上游媒体/记者标识或内部评分。 |
| 外部媒体提示 | 媒体搜索不再把第三方运行提示返回给客户。无论结果来自最近成功缓存还是当前查询，客户仅看到“候选资料待项目核验”的边界说明；这不表示受邀、出席或报道承诺。 |
| 客户任务与台账界面 | 项目列表、任务列表、任务记录、订单管理和项目详情均不再渲染负责人列或“待分配”占位。该字段属于内部协同信息；客户只查看项目、服务、金额、状态、时间与可见说明。 |
| 客户状态语义 | 客户侧把内部状态 `PENDING_ASSIGNMENT` 呈现为“待平台安排”，不再显示“待分配”；任务管理空状态不再提及平台工单。订单管理、任务管理、任务记录在 1280×720 和 390×844 下复测均未出现供应商、成本、上游、令牌、平台工单或“待分配”文字。 |
| 客户结算台账 | 新增客户限定的 `settlement-records` 读取接口和订单管理账务区，只展示已有、平台确认的结算信息。供应商、成本、运营人、上游标识和内部对账信息均不返回；页面不提供或暗示在线支付、退款、开票或外部对账能力。 |
| Java 构建与测试 | `backend\\mvnw.cmd -q test` 通过：58 项测试，失败 0、错误 0、跳过 0。 |
| 前端构建 | `frontend\\npm.cmd run build` 通过，Vue 类型检查与 Vite 生产构建均完成。 |
| Docker 与配置 | 本机演示 Compose 重新构建并运行，前端、后端、PostgreSQL、Redis 均健康；健康接口返回 `schemaStatus=UP`、`schemaVersion=20` 和 API 合同 `winpress-v4.2.1-20260729`。本机与生产 Compose 均完成静态配置校验，生产 Compose 未启动。 |
| 干净数据库重建 | `scripts\\verify-clean-local-db.ps1` 在不开放宿主机端口、自动清理的临时 PostgreSQL 容器中通过：四服务关键表、活动关联字段和稿件来源字段均存在；受控 CSV 导入 22,364 个渠道与 22,363 条报价；7 个本机演示账号的 bcrypt 哈希均与受控账号说明匹配。该结果仅证明静态种子可重建，不证明外部媒体授权、实时检索或供应商履约。 |
| 存量演示卷边界 | 当前本机复用数据卷保留 420 条 `LEGACY_MEDIA` 历史渠道及对应报价，均为 `REVIEW_REQUIRED`；客户渠道目录仅查询 `ACTIVE` 状态，且回归新增来源、成本、供应商与内部标识断言。未对历史记录自动删除、改价或改为可下单状态。 |
| 源码归档 | 当前源码包由 `scripts/package-current-source.ps1` 生成，逐项校验 304 个源码条目、必需部署文件与 SHA-256；环境文件、运行生成物、演示数据、日志、测试账号说明和历史快照不随包交付。具体包名见 [`SOURCE-PACKAGE-MANIFEST-20260728.md`](SOURCE-PACKAGE-MANIFEST-20260728.md)。 |
| 接口、数据库与权限 | `scripts\\verify-local-p0p1.ps1` 通过：健康结构、三类角色与四个品牌案例客户登录、附件与静态路径、客户项目/任务/候选字段投影、客户订单与结算字段边界、跨客户项目拒绝、运营越权拒绝、客户与发布运营对后台报价/供应商/供应商订单接口的拒绝，以及平台管理员的受控后台访问均已复测。脚本使用 UTF-8 JSON 请求，覆盖浏览器登录的非 ASCII 密码输入。`-CreateActivityClosure` 额外验证四个独立项目均有任务与订单：云采写一人一天客户台账金额为 ¥980；发布会、媒体邀请及未选渠道的直编发稿保持“待项目确认”，不伪造价格。 |
| 稿件来源与本机样本 | 回归脚本已检查“客户可见来源标签、内部来源 ID 不可见”。本机 QA 样本去除客户可见的供应商内部措辞；源数据与当前本机演示库均已回读核验。该样本只用于本机功能回归，不构成对外发布或真实履约证据。 |
| 页面跨端烟测 | 已用本机 Chrome 直接访问 Docker 前端 `5217` 复核：首页在 1440×900 下完整显示；以真实 390×844 设备指标复核时，标题无截断、导航按钮可见，打开后显示 6 个频道入口且页面滚动锁定。登录页也已在 390×844 下截图复核。登录后的直编发稿项目页已在 1440×900 与 390×844 实测：来源标签可见，未出现供应商、成本或上游字段，且无横向溢出；媒体邀请页面显示未授权时的人工补充路径；新闻发布会在 1440×900 与 390×844 下保持仅三项必填、筛选不可用提示与无横向溢出；直编发稿和云采写在 390×844 下均无横向溢出。截图存放于本机可视化验收目录；预发布环境仍应补齐全量自动化回归。 |
| 客户深链接与最新跨端复测 | 为避免书签、消息链接或旧入口落回营销首页，`/requirements/onsite-writing` 兼容映射到云采写下单，`/task-records` 兼容映射到任务记录。重建本机 Docker 前端后，以客户会话实测两个路径在 1280×720 正常加载、无横向溢出、无内部字段和控制台错误；再以真实 390×844 视口复测云采写、媒体邀请、直编发稿、新闻发布会、任务记录和订单管理，均正确加载、无横向溢出、无内部字段和控制台错误。 |
| 四服务本轮闭环复测 | `scripts\\verify-local-p0p1.ps1 -CreateActivityClosure` 在本机演示库再次通过：以同一客户会话创建发布会根项目及云采写、媒体邀请、直编发稿三个独立关联项目；四项服务均保留独立项目、任务记录与订单记录，直编发稿只复制客户已确认稿件的可读来源标签。该数据仅用于本机回归，不构成真实活动、真实媒体资源或真实履约。 |

## 2026-07-29 状态、计价与部署一致性复核

| 检查 | 结果 |
|---|---|
| 独立服务项目绑定 | 保存计划前服务端分别核验项目类型：媒体邀请计划只能绑定 `MEDIA_PR` 项目，直编发稿计划只能绑定 `DIRECT_PUBLISHING` 项目；发布会根项目及其他服务项目均被拒绝。前端选择器同步只显示本服务项目。 |
| 媒体邀请事实状态 | 确认邀请计划只创建任务和待处理邀请记录，不写入“已邀请”时间；只有获授权项目专员可在实际发生后记录已邀请、已回复、未参与或到场。客户不能调用该记录接口，且客户投影不返回该内部协同字段。 |
| 异常不伪造媒体回复 | 媒体邀请任务发生通用异常时，不再把异常自动写成媒体拒绝或回复；没有稿件来源的人工邀请任务也不会因释放锁而发生空值错误。成果链接经受控提交后才标记为“成果已核验”，不以回复时间替代成果证据。 |
| 直编发稿计价语义 | 计划确认仍须使用客户范围内的已确认稿件和有效渠道报价；客户订单页把进行中直编发稿金额标为“计划价 / 金额”并显示“待项目核验”，不把该数值表述为供应商已确认价格、库存或排期。 |
| 本机 Docker 构建一致性 | 本机演示后端镜像在 Compose 重建前必须先运行 `scripts\\prepare-local-docker-backend.ps1`；该脚本在临时副本中完成测试与打包，避免 `Dockerfile.local` 复制历史 JAR 造成源码与运行镜像不一致。生产 Dockerfile 的源码构建仍待预发布验收。 |
| 前后端与接口回归 | 本轮修改后 `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 通过，63 项测试、失败 0、错误 0、跳过 0；`scripts\\verify-local-p0p1.ps1` 的不写入业务数据回归通过。此前受控的 `-CreateActivityClosure` 闭环结果仍仅存在于本机演示库。 |
| 数据库、部署与跨端页面 | 本机健康检查返回后端、数据库和结构均为 `UP`，结构版本为 `20`；干净数据库重建与生产 Compose 静态解析均通过，未启动生产容器。媒体邀请、直编发稿及订单管理在桌面和 390px 级移动视口复测均无页面横向溢出、无控制台错误，且不显示供应商、成本、上游接口、令牌或内部规则。订单表在移动端保留容器内横向滚动，页面本身不溢出。 |

### 本轮部署限制

Docker 内 Maven Central 曾出现短暂 TLS 握手失败。为保证本机演示的可重复性，`docker-compose.local-demo.yml` 的后端使用 `Dockerfile.local`，该镜像只复制由 `scripts/prepare-local-docker-backend.ps1` 在临时源码副本中完成测试和打包后生成的本机 JAR。生产 Dockerfile 仍从源码构建，并增加有限下载重试；生产镜像构建、迁移和发布尚未验收，不能由本机演示结果替代。

## 2026-07-29 生产数据边界与移动端复测

| 检查 | 结果 |
|---|---|
| 生产媒体目录边界 | `docker-compose.production.yml` 不再自动挂载 `03-media-import.sql`、`04-normalize-public-channel-data.sql`、媒体 CSV 或报价 CSV。`scripts\\verify-production-compose-boundaries.ps1` 静态解析通过，确认未授权目录、报价和演示供应商不会进入生产首建。生产容器未启动。 |
| 演示供应商边界 | 迁移 `14-supplier-orders-and-conference-workbench.sql` 不再写入“本机演示直编服务商”或其渠道关系。供应商、成本和上游履约能力仍只在受控后台模型内，不能对客户或生产声明为已接通。 |
| 直编目录待确认状态 | 当直编目录没有经过批准的对客数据且客户未施加筛选条件时，页面显示“渠道目录待确认”与创建独立直编项目的入口，不提供空的选择或提交控件。该状态当时经源码和前端构建核验；本记录后续章节已补齐生产等价冷启动，正式外部目录授权仍待确认。 |
| 干净数据库 | `scripts\\verify-clean-local-db.ps1` 通过：临时隔离库包含四服务关键结构，受控静态导入为 22,364 个渠道、22,363 条报价。此结果仅证明本机静态种子完整性，不证明第三方数据授权、实时检索、价格有效性或供应商履约。 |
| 最终构建 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 通过，63 项测试、失败 0、错误 0、跳过 0。 |
| 最终接口与权限回归 | `scripts\\verify-local-p0p1.ps1` 通过：数据库结构状态、匿名附件拒绝、三类角色边界、客户字段白名单、跨客户项目拒绝、后台计价与供应商接口拒绝、未授权媒体状态均为预期。 |
| 跨端页面复测 | Docker 前端重建后，登录客户的直编发稿页在 1440×900 与 375px 级移动视口无页面横向溢出、无控制台 warning/error；移动端筛选控件和“选择渠道”按钮均为 44px。渠道表如需横向空间，仅在自身容器内滚动。 |

本节当时不替代生产冷启动与恢复；本记录后续章节已补齐本机隔离的生产等价冷启动和非空成对恢复。外部数据/供应商授权、法律主体、真实生产加密异地备份、TLS、域名与监控仍按“待确认”处理。

## 2026-07-29 人工媒体名单待核验闭环复测

| 检查 | 结果 |
|---|---|
| P0 根因与修复 | 外部媒体资料库未验收时，旧前端仍可能把人工补充名单绑定到首个静态 `MEDIA_PR` 渠道。现已取消该隐式绑定：人工名单始终以空执行渠道保存，后端只在 `MEDIA_PR` 中接受该形式并标记为 `MANUAL_REVIEW`。 |
| 业务状态 | 保存人工名单只建立待确认计划；确认后创建待处理媒体邀请任务和客户可见的未报价订单记录。不会创建供应商订单、成本记录或“已邀请”时间，也不表示媒体已同意参与、采访或报道。 |
| 数据与权限 | `verify-local-p0p1.ps1 -CreateActivityClosure` 通过：人工名单无执行渠道仍可完成计划确认与任务建立；客户只能查看自身任务和订单，不能记录媒体联系进度，也看不到供应商、成本、上游、令牌、内部备注或操作人字段。 |
| 构建与运行镜像 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 通过，8 个测试报告、65 项测试、失败 0、错误 0、跳过 0。随后执行 `prepare-local-docker-backend.ps1` 并重建本机前后端容器，避免运行镜像落后于源码。 |
| 数据库与部署 | 健康接口返回 `schemaStatus=UP`、`schemaVersion=21`、API 合同 `winpress-v4.2.2-20260729`。`verify-clean-local-db.ps1` 通过（22,364 个受控静态渠道、22,363 条报价、4 个服务受理任务、7 个本机演示账号）；`verify-cold-start.ps1` 通过（41 张表）；生产 Compose 数据边界静态检查通过，且未启动生产容器。静态目录完整性不构成外部数据授权或实时履约证明。 |
| 页面跨端复测 | Docker 前端的媒体邀请页在 1440×900 和 390×844 复测无横向溢出、无控制台错误。移动端“人工补充拟邀对象”和“提交媒体邀请”均为 44px；人工名单进入确认页后显示“待核验”与“不自动向媒体发出邀请”，本次浏览器验证未保存任何计划。 |

本节的本机演示数据与回归结果不能替代外部媒体数据授权、供应商合同、法律信息、生产凭据、预发布验收或生产发布。

## 2026-07-29 P1 任务页角色与触控复测

| 检查 | 结果 |
|---|---|
| 客户状态语义 | 客户任务页将内部 `PENDING_ASSIGNMENT` 显示为“待平台安排”；表头显示“服务阶段”，不再将内部执行归口作为客户信息。 |
| 客户验收权限 | “确认验收”按钮仅对客户角色渲染。新增服务测试与本机接口回归：发布运营调用 `POST /publish-tasks/999999/accept` 返回 `403`；该断言在任务不存在时仍先验证角色边界，不读取任务内容。 |
| 移动端触控 | 浏览器实际测得修复前任务表文字按钮为 32px；修复后 390×844 下“确认验收”为 44px，高度满足触控基线。表格仍仅在自身容器内处理横向空间，页面与正文均无横向溢出。 |
| 桌面与移动页面 | 使用客户会话在 1440×900 与 390×844 复测任务页：状态文案正确、客户不见“待分配”、桌面显示服务阶段、两种视口均无控制台 warning/error。 |
| 构建、数据库与权限 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 通过（66 项，失败 0、错误 0、跳过 0）。本机 Docker 前后端已从当前源码重建；健康接口返回数据库与结构 `UP`、版本 `21`、合同 `winpress-v4.2.2-20260729`；`verify-local-p0p1.ps1` 通过，包含客户字段白名单、跨客户拒绝、后台计价/供应商边界和新增的运营验收拒绝。 |
| 源码包卫生 | 打包脚本已排除 `frontend/tmp` Vite 缓存、根临时目录、LibreOffice QA 配置目录、构建产物、日志、探针文件、环境文件和账号说明。工作区文件不被删除；新的包只收录 228 个受控源码文件，并由包外 `.sha256` 校验。 |
| 四服务独立闭环 | 在本机隔离演示库重新执行 `verify-local-p0p1.ps1 -CreateActivityClosure`：现场采写、媒体邀请、直编发稿、新闻发布会均分别经过下单、项目、任务记录和订单记录；云采写一人一天为 ¥980；人工媒体邀请仍为未报价、待项目核验，且不自动记录为已邀请。该回归数据不构成真实媒体、供应商或履约声明。 |

该复测仅针对本机演示环境，不构成真实媒体数据、供应商履约、法律主体、生产凭据或生产发布验收。

## 2026-07-29 P1 本机演示标识与生产编译隔离

| 检查 | 结果 |
|---|---|
| 本机环境提示 | 前端仅在 `VITE_LOCAL_DEMO=true` 的构建中显示“本机演示环境”；提示明确示例与测试数据仅用于功能验证，不代表生产服务、媒体资源或履约结果。 |
| 生产编译边界 | `docker-compose.production.yml` 明确传入 `VITE_LOCAL_DEMO=false`；生产 Compose 数据边界脚本同步断言该值，避免把本机演示标识或编译配置误带入生产镜像。默认前端生产构建的输出不包含“本机演示环境”提示文本。 |
| 数据边界 | 环境提示不读取、导出或披露供应商、成本、上游接口、令牌或内部规则；它只说明当前部署的数据性质。外部媒体、供应商、法律信息和生产凭据仍保持待确认。 |
| 构建、接口与数据库回归 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 共 66 项、失败 0、错误 0、跳过 0；本机 Docker 前后端重建后健康接口返回数据库与结构 `UP`、版本 `21`、合同 `winpress-v4.2.2-20260729`；`verify-local-p0p1.ps1 -CreateActivityClosure` 与干净本机数据库重建均通过。 |
| 生产配置与跨端页面 | `verify-production-compose-boundaries.ps1` 通过，生产 Compose 未启动。浏览器以客户会话在 1440×900 与 390×844 复测：本机提示可见，页面和正文无横向溢出，控制台无 warning/error；提示不显示供应商、成本、上游接口、令牌或内部规则。 |

## 2026-07-29 P1 客户台账边界与跨端复测

| 检查 | 结果 |
|---|---|
| 客户台账字段 | 客户 `work-items`、`task-records`、`order-records` 只投影公开记录号、项目、服务、状态、时间和客户说明；响应不再包含 `itemType` 或 `itemId`。前端以公开 `recordNo` 作为列表标识，不渲染内部对象类型。 |
| 客户状态语义 | 状态组件已将待项目确认、待补充资料、待项目核验、成果待核验等阶段映射为客户可读文本；任何未登记状态统一显示“状态待更新”，不回退输出原始枚举。 |
| 移动端触控 | 使用客户会话在 390×844 实测：普通图标控制、账号入口、表格文字链接和分页控制均达到至少 44px。修复前订单页有 45 个小于 44px 的目标，修复后为 0。 |
| 桌面与移动页面 | 客户订单页在 1440×900 与 390×844 复测：无内部状态码、对象类型或内部 ID，无页面横向溢出，控制台无 warning/error。 |
| 构建、数据库与权限 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 通过（66 项，失败 0、错误 0、跳过 0）；本机 Docker 从当前源码重建后，健康接口返回数据库与结构 `UP`、版本 `21`、合同 `winpress-v4.2.2-20260729`；`scripts\\verify-local-p0p1.ps1` 通过，包含客户字段投影、跨客户拒绝和后台计价/供应商接口隔离。 |

该复测仅证明本机演示环境的客户数据投影、权限边界和跨端页面行为；它不构成外部媒体数据、供应商履约、法律主体、生产凭据或生产发布验收。

## 2026-07-29 P0 客户发布任务公开标识与最终跨端复测

| 检查 | 结果 |
|---|---|
| 客户发布任务标识 | `GET /publish-tasks` 的客户投影不再包含数据库 `id`；客户列表、详情和确认均使用公开 `taskNo`。字段边界回归已验证没有供应商字段或内部任务主键。 |
| 计划确认响应 | `POST /publish-plans/{planId}/confirm` 仅返回 `status`、`taskNos` 和提示文本；不再向客户返回 `planId` 或 `taskIds`。前端的新任务定位参数仅使用公开任务号。 |
| 路由权限 | 带内部主键的 `/publish-tasks/{taskId}` 仅限 `PUBLISH_OPERATOR` 与 `PLATFORM_ADMIN`；客户使用 `/customer/publish-tasks/{taskNo}`。客户确认接口按 `taskNo` 处理，服务端仍在权限校验后解析内部记录。 |
| 四服务闭环 | 本轮前置闭环回归已实际创建云采写、媒体邀请、直编发稿、新闻发布会四个独立服务项目，并验证各自项目、任务与订单记录；本次最终复测未改变该业务逻辑。 |
| 本机数据库、接口与权限 | 当前 Docker 重建后，健康接口返回数据库与结构 `UP`、版本 `21`、合同 `winpress-v4.2.2-20260729`。`scripts\\verify-local-p0p1.ps1` 通过，包含客户/运营/管理员权限、跨客户拒绝、价格和供应商后台隔离，以及未授权外部媒体保持不可用。 |
| 桌面与移动页面 | 客户任务页在 1440×900 与 390×844 复测：资源正常、无页面横向溢出、不显示原始任务主键或状态枚举。最终把窄屏菜单按钮的 `flex-basis` 调整为 44px 后，390px 下小于 44px 的可见交互目标为 0。 |
| 生产配置边界 | `verify-production-compose-boundaries.ps1` 通过；生产 Compose 未启动，也不会自动导入演示夹具或未验收媒体目录。 |

本节仅记录本机演示环境的代码、接口和页面回归。真实媒体数据许可、供应商履约、法律主体和生产凭据仍未验收，不能据此对外表述为已具备。

## 2026-07-29 P1 传播看板指标与移动端入口复测

| 检查 | 结果 |
|---|---|
| 缺失统计字段 | 前端不再将缺失、非数值或异常的看板统计默认为 `0`；统一显示“数据暂不可用”。本机接口当前完整返回统计值，因此页面实测不显示该兜底文字；该逻辑由源码和 Vue 生产构建验证。 |
| 客户看板指标一致性 | `verify-local-p0p1.ps1` 逐项比对 `todoCount`、`activeProjects`、`pendingTasks`、`taskCount`、`awaitingAcceptanceTasks`、`mediaInvitationTasks`、`directPublishingTasks`、`writingAssignments`、`conferenceProjects` 与其点击后客户范围列表的 `total`，九项均通过。 |
| 数据边界 | 客户看板响应仅包含角色范围内的计数；回归确认不包含供应商、成本、上游、令牌、内部规则或运营资料。后台计价、供应商及供应商订单接口继续拒绝客户与发布运营。 |
| 桌面与移动页面 | 客户传播看板在 1440×900 与 390×844 实测：资源正常、卡片布局正常、无横向溢出、不显示原始状态码或内部字段。390px 下将“提交新需求”和文字型查看链接提升到 44px 后，可见交互目标小于 44px 的数量为 0。 |
| 构建、数据库与配置边界 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 通过（66 项，失败 0、错误 0、跳过 0）；本机 Docker 重建后，健康接口数据库与结构均为 `UP`、版本 `21`、合同 `winpress-v4.2.2-20260729`；本机权限/数据库/接口回归和生产 Compose 静态边界检查均通过。 |

本节只证明本机演示部署的看板口径、代码与页面行为。真实媒体数据、供应商履约、法律信息与生产凭据仍未验收。

## 2026-07-29 P0 生产部署预检与迁移清单一致性

| 检查 | 结果 |
|---|---|
| 生产迁移说明 | 部署说明的手动首建清单补入迁移 `21-manual-media-invitation-pending-verification.sql`，与 `docker-compose.production.yml` 的挂载顺序一致。 |
| 环境文件边界 | 新增 `scripts\\verify-production-readiness.ps1 -EnvFile <受保护环境文件>`。它拒绝 `.env.example`、必填变量缺失或占位符，且不打印数据库密码、Redis 密码或第三方令牌。 |
| CORS、外部接口与端口 | 预检要求 HTTPS 的公共 CORS 来源；外部媒体地址与令牌必须同时配置或同时留空；PostgreSQL、Redis、前端和后端端口必须合法、互异且未被宿主机占用。 |
| 静态验证 | `.env.example` 的拒绝路径已按预期验证。使用不含真实凭据、未占用端口的临时环境文件，预检、端口检查与 `docker compose ... config --quiet` 均通过；不启动容器、不连接生产数据库。 |
| 生产边界 | `verify-production-compose-boundaries.ps1` 继续通过，生产 Compose 不会自动导入本机种子、案例、未验收媒体目录、报价或演示供应商。 |
| 干净数据库重建 | `verify-clean-local-db.ps1` 在无宿主机端口映射的临时容器通过：关键结构齐备、4 类服务受理任务齐备、7 个本机演示账号哈希可复核，静态渠道 22,364 条、静态报价 22,363 条。该结果只证明受控本机种子和迁移可重建，不等于外部数据许可或供应商履约。 |

真实生产环境文件、备份恢复、TLS、监控、域名切换、外部数据授权和书面上线批准未作为本轮输入，仍属待确认，不能据此声称生产已验收。

## 2026-07-29 P1 四项独立服务项目级闭环复测

| 检查 | 结果 |
|---|---|
| 项目与任务映射 | 在一组活动关联下，新闻发布会、云采写、媒体邀请、直编发稿分别创建独立项目；客户任务记录逐项目匹配“新闻发布会、云采写、媒体邀请、直编发稿”四种客户类别。 |
| 项目与订单映射 | 四个项目分别且仅分别拥有 `NEWS_CONFERENCE`、`ONSITE_WRITING`、`MEDIA_PR`、`DIRECT_PUBLISHING` 一笔订单记录；同一活动关联不生成合并订单或重复订单。 |
| 计价与待确认边界 | 云采写一位写手一天的客户账单为 ¥980；人工媒体邀请保持未报价、待项目核验，不自动写入媒体已邀请、供应商订单、成本或外部执行结果。 |
| 回归脚本可靠性 | 回归改为显式外层项目映射循环，避免 PowerShell 嵌套筛选变量遮蔽。脚本以 UTF-8 BOM 保存，并经 PowerShell 语法解析后执行，确保 Windows 本机中文客户标签不被代码页截断。 |
| 本机接口、数据库与权限 | `scripts\\verify-local-p0p1.ps1 -CreateActivityClosure` 完整通过；它同时覆盖健康、结构版本、客户字段投影、跨客户拒绝、后台计价/供应商隔离、未授权外部媒体保持不可用及本节四服务项目级映射。测试仅在本机 Docker 演示库执行。 |
| 构建、配置与页面可达 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 的 Surefire 统计为 66 项、失败 0、错误 0、跳过 0；`verify-production-compose-boundaries.ps1` 通过。首页在本机 1280×720 验证可加载，未出现 Vite 错误覆盖层。本轮未改动 Vue 页面，因此既有 1440×900、390×844 视觉回归保持原证据，不把它表述为生产验收。 |

本节不构成真实媒体数据、真实供应商联动、法律主体或生产履约验收。外部能力在取得授权、凭据、沙箱/生产联调和书面验收前持续标注为待确认。

## 2026-07-29 P1 发布会最小受理与项目清单复测

| 检查 | 结果 |
|---|---|
| 最小首次提交 | 本机以仅含需求标题、会务联系人、联系电话的客户请求创建新闻发布会项目。活动时间、地点、规模、媒体目标均为空，接口仍返回成功项目；这证明首次受理不强迫客户填写额外资料。 |
| 九项执行清单 | 新项目自动形成 9 项清单，排序为 1—9：6 项 `PRE_EVENT`、1 项 `ONSITE`、2 项 `POST_EVENT`。未填活动时间时各项日期为空，避免伪造排期。 |
| 客户信息边界 | 客户响应中的清单只含公开事项号、顺序、阶段、标题、说明、日期、状态与更新时间；回归明确拒绝 `id`、负责人、运营备注等内部字段。没有由本次最小提交生成外部媒体查询、供应商订单或媒体已邀请事实。 |
| 四服务关系 | 该发布会项目仍只作为活动关联根；云采写、媒体邀请和直编发稿继续分别下单、分别创建项目/任务/订单，不能被九项清单合并为组合服务。 |
| 活动关联权限 | 使用另一客户会话伪造该发布会 `relatedProjectId` 创建媒体邀请服务，接口返回 `400 RELATED_PROJECT_INVALID`。拒绝路径不创建跨客户项目、任务或订单；关联同时要求同一客户与同一组织。 |
| 页面可达与必填提示 | 已登录客户会话在 `/requirements/news-conference` 的 1280×720 本机浏览器检查中显示“举办新闻发布会”，只识别到需求标题、会务联系人、联系电话 3 个必填标签，且显示九项清单说明、无 Vite 错误覆盖层。本轮未改 Vue 页面，故既有 390×844 移动端记录维持原证据，不被这一桌面实测替代。 |
| 构建与配置 | `scripts\\verify-local-p0p1.ps1 -CreateActivityClosure`、`frontend\\npm.cmd run build`、`backend\\mvnw.cmd -q test`（66 项、0 失败、0 错误、0 跳过）及 `verify-production-compose-boundaries.ps1` 均通过。生产容器和任何外部能力均未启动。 |

本节只记录本机演示数据库的受理和项目协同回归。外部媒体数据、供应商履约、法律信息与生产凭据仍待确认。

## 2026-07-29 P0 直编发稿稿件来源客户隔离复测

| 检查 | 结果 |
|---|---|
| 来源清单字段 | `GET /customer/approved-manuscripts` 仅返回启动直编项目需要的客户范围引用与标签；回归拒绝正文、摘要、修改备注、来源链、成本、供应商和上游字段。 |
| 跨客户列表隔离 | 第二客户会话无法在自己的来源清单中看到第一客户的稿件 ID 或版本 ID。 |
| 跨客户引用拒绝 | 第二客户伪造第一客户稿件与版本 ID 创建直编发稿，接口返回 `400 SOURCE_MANUSCRIPT_NOT_AVAILABLE`；请求不会创建或复制直编项目内容。 |
| 全链路回归 | `scripts\\verify-local-p0p1.ps1 -CreateActivityClosure` 完整通过，四项服务的项目、任务、订单闭环和客户字段投影继续通过。外部媒体、渠道库存、供应商报价及生产凭据均未参与此测试。 |

本节仅确认本机演示数据库中的客户范围、稿件复制与接口边界；不构成外部渠道或生产履约能力验收。

## 2026-07-29 P1 直编计划稿件项目归属双重校验

| 检查 | 结果 |
|---|---|
| 确认阶段防护 | `confirmPublishPlan` 现在会核验计划的 `projectId` 与稿件版本的 `project_id` 一致。若历史数据或异常写入造成不一致，返回 `MANUSCRIPT_PROJECT_MISMATCH`，不会创建发布任务或确认计划。 |
| 同客户跨项目拒绝 | `verify-local-p0p1.ps1 -CreateActivityClosure` 创建两个独立直编项目，再将第一个项目的已确认复制稿件用于第二个项目的计划请求。接口返回 `404 NOT_FOUND`；请求在计划、任务和订单写入前被拒绝。 |
| 客户与外部边界 | 该验证不改变客户可见稿件范围，也不披露来源内部标识、供应商、成本、上游接口或令牌。外部渠道、供应商报价、库存与履约均未参与此回归，持续保持待确认。 |
| 构建、运行与权限 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 共 67 项、失败 0、错误 0、跳过 0；本机 Docker 重建后健康接口返回数据库与结构 `UP`、版本 `21`、合同 `winpress-v4.2.2-20260729`；完整本机闭环、客户/运营/管理员权限和生产 Compose 静态边界检查均通过。 |
| 桌面与移动页面 | 本次仅改后端保护与回归脚本。仍以当前 5217 Docker 首页重新在 1440×900、390×844 检查可达性：无页面横向溢出、无 Vite 错误覆盖层；390px 菜单可展开且按钮 44px。该首页复核不替代其他业务页的预发布验收。 |

本节仅记录本机演示环境的代码、接口、权限和首页可达性证据；不构成外部媒体、供应商、法律信息、生产凭据或生产发布验收。

## 2026-07-29 P1 历史组合服务归档边界

| 检查 | 结果 |
|---|---|
| 保留范围 | 历史 `WRITING_AND_PUBLISHING` 项目仍保留为客户可查的归档记录；本轮没有拆分、改价、迁移或删除历史需求、项目、任务或订单。 |
| 新链路阻断 | `activityRootProjectId` 只接受四项当前独立服务的项目根。以本机种子历史项目作为 `relatedProjectId` 创建媒体邀请，接口返回 `400 RELATED_PROJECT_INVALID`，不创建新需求、项目、任务或订单。 |
| 页面语义 | 历史项目详情显示“历史组合记录（仅归档）”；“新增服务”卡片只对当前四项服务显示，历史项目不再出现该入口。 |
| 桌面与移动 | 当前 Docker 的 `/projects/1` 在 1440×900、390×844 实测均显示归档标签、不显示新增服务、无页面横向溢出、无 Vite 错误覆盖层；390px 菜单按钮为 44px。此为本机演示页面证据。 |
| 构建、数据库与权限 | `frontend\\npm.cmd run build` 通过；`backend\\mvnw.cmd -q test` 共 67 项、失败 0、错误 0、跳过 0；当前 Docker 健康接口数据库与结构均为 `UP`、版本 `21`、合同 `winpress-v4.2.2-20260729`；完整 `verify-local-p0p1.ps1 -CreateActivityClosure` 通过。 |

本节只确认历史记录不再进入新服务活动关联链路。历史业务映射、迁移或归档决定仍需业务书面确认，不能自动处理。

## 2026-07-29 P1 历史资料与演示数据口径复核

| 检查 | 结果 |
|---|---|
| 当前验收索引 | 根 README 与 `docs/README.md` 将 V4.2 PRD、全局审查、P0/P1 执行记录和源码包清单列为当前入口；旧版 PRD、阶段报告和历史 QA 均标明仅供追溯。 |
| 历史结论隔离 | V4.1/2026-07-26 文档不再作为当前源码、镜像、外部数据、供应商、法律信息或生产部署的验收依据。各文档均回链到当前 V4.2 验收口径。 |
| 演示数据计数 | 只读本机演示库汇总：22,784 条渠道、22,783 条报价和商品；其中 22,364 条渠道、22,363 条报价和商品为 `ACTIVE`，420 条均为 `REVIEW_REQUIRED` 历史记录。干净重建仍以 22,364/22,363 的受控静态输入为完整性证据。 |
| 数据与客户边界 | 文档明确计数不代表外部数据授权、实时价格、媒体可约、供应商合作或生产履约；客户侧继续只能使用当前权限内的项目、任务、订单与待项目核验说明。 |
| 文档可追溯性 | 对根 README、文档索引、数据库说明和 8 份历史材料执行本地相对链接检查，共 39 条链接通过；未修改测试账号说明或环境凭据。 |
| 前后端构建 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（67 项，失败 0、错误 0、跳过 0）。 |
| 数据库、接口与权限 | `verify-local-p0p1.ps1 -CreateActivityClosure` 通过：健康与结构就绪、四服务独立项目/任务/订单、客户字段白名单、跨客户拒绝、后台计价与供应商隔离、未授权媒体不可用均符合预期。 |
| 部署边界 | `verify-production-compose-boundaries.ps1` 静态校验通过；生产 Compose 未启动，且不会自动加载演示夹具或未验收媒体目录。 |
| 桌面与移动页面 | 首页在 1440×900 与 390×844 复测可正常加载；无页面横向溢出、无 Vite 错误覆盖层，图片资源完整，移动导航按钮为 44×44 px，控制台未记录 warning/error。 |

本节属于文档与审查口径修复，并已按当前源码完成构建、数据库/API/权限和跨端页面回归。外部媒体、供应商、法律信息和生产凭据继续保持待确认。

## 2026-07-29 P1 独立服务与活动关联的对客语义及客户字段边界

| 检查 | 结果 |
|---|---|
| 修复范围 | 首页、关于页、首页服务内容与案例卡片删除“多个订单归入一个项目”“同一张项目清单”等组合项目暗示，改为同一活动下的独立服务项目关联查看；项目、任务和订单仍分别保留。 |
| 发布会边界 | 发布会文案只说明会务清单、现场安排、拟邀名单与会后事项；需要采写、媒体邀请或直编发稿时，明确另建独立服务项目。 |
| 对客文案 | 案例卡片从“云发布的处理 / 客户获得的交付”改为“客户现状 / 执行安排 / 可查交付”；云采写不再被表述为包含发布追踪，媒体邀请和发布会不承诺媒体实际参与或报道。 |
| 客户字段边界 | 客户项目、订单、任务、媒体候选和媒体状态响应经字段投影后不含内部执行人、候选主键、上游媒体/记者标识、内部备注、图片源地址、供应商/成本、令牌或适配器配置原因；运营方不得枚举客户需求。 |
| 媒体邀请提示 | 候选状态更新时客户只看到“媒体筛选状态已更新，请返回媒体列表重新筛选”，不显示“检索凭据”等鉴权概念。未授权在线筛选仍仅提供人工补充与执行前核验路径。 |
| 前后端构建 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（67 项，失败 0、错误 0、跳过 0）。 |
| 本机运行与权限 | 当前 Docker 前后端重建并健康；`verify-local-p0p1.ps1 -CreateActivityClosure` 通过四服务独立下单、项目、任务、订单、客户投影、跨客户拒绝、后台价格/供应商隔离及未授权媒体不可用回归。生产 Compose 数据边界静态校验通过且未启动生产容器。 |
| 干净数据库重建 | `verify-clean-local-db.ps1` 在临时隔离容器通过：四类服务受理任务齐备，22,364 条受控静态渠道、22,363 条静态报价与 7 个本机演示账号可重建。该结果只说明本机结构与种子完整性。 |
| 运行资源核验 | 从当前 Docker 前端实际加载的首页、关于页及共享营销 JS 资源中读取到新文案，证明运行镜像已包含本次前端修改。 |
| 跨端图面复测 | 当前 Docker 首页、关于页与媒体邀请页在 1440×900、390×844 实测无页面横向溢出、无 Vite 错误覆盖层，图片资源完整；390px 导航菜单为 44×44 px，控制台 warning/error 为空。媒体邀请页不显示“检索凭据”、供应商状态、限流、重试窗口、图片源地址或上游媒体信息。该证据仅覆盖本机演示部署。 |

本节不改变四项服务、客户字段和外部能力边界。外部媒体、供应商、法律信息和生产凭据仍为待确认，不能以本机演示、内部记录或营销文案替代。

## 2026-07-29 P1 媒体邀请计划边界与表单复核

| 检查 | 结果 |
|---|---|
| 新建计划边界 | 当前客户媒体邀请只保存拟邀名单并提交项目核验；页面不再提供排他邀约或锁定截止时间。计划目标改为“保存拟邀名单，待项目核验”，确认标题为“核对媒体邀请计划”。 |
| 服务端拒绝 | 向媒体邀请计划接口提交 `exclusiveMediaPr=true` 与锁定时间，返回 `400 MEDIA_PR_EXCLUSIVE_NOT_AVAILABLE`；回归确认失败请求不生成计划、任务或订单。 |
| 客户字段投影 | 客户读取发布计划时不返回 `exclusiveMediaPr` 或 `lockExpiresAt`；遗留锁定记录只保留后台兼容，不作为客户自助能力。 |
| 存量保护 | 既有安排仍可阻止冲突的直编计划，但客户错误提示不披露旧条款细节，只提示联系项目负责人确认。 |
| 交互与跨端 | Docker 媒体邀请页在 1440×900、390×844 下无页面横向溢出、无 Vite 错误覆盖层、无破损图片。人工补充的“媒体名称*”保持同一行；390px 下人工补充与提交按钮均为 44px。页面不显示排他入口或“按拟邀名单发出邀请并跟进回复”。 |
| 构建、运行与权限 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（67 项、失败 0、错误 0、跳过 0）。健康接口返回数据库/结构 `UP`、版本 `21`、合同 `winpress-v4.2.3-20260729`；`verify-local-p0p1.ps1 -CreateActivityClosure`、干净数据库重建和生产 Compose 边界静态校验均通过。 |

本节只验证本机演示中的客户计划边界、字段投影和页面交互；外部媒体数据、真实供应商联动、法律信息、生产凭据和生产部署仍为待确认。

## 2026-07-29 P1 媒体邀请事实留痕与任务页复测

| 检查 | 结果 |
|---|---|
| 事实登记约束 | 媒体邀请的状态从待处理变更为已邀请、已回复、确认到场、婉拒或不再继续时，服务端要求运营人员填写简要事实说明；空说明返回 `400 MEDIA_INVITATION_NOTE_REQUIRED`。同状态重复请求仍保持幂等，不写入新的事实。 |
| 状态机优先级 | “未发出邀请即登记回复”等非法跳转，继续优先返回 `INVALID_MEDIA_INVITATION_TRANSITION`，不会被空说明校验掩盖。 |
| 客户数据边界 | 沟通说明只保存在运营协同记录中；客户任务接口不返回沟通状态、邀请时间、执行备注、负责人、供应商、成本、上游标识或内部任务主键。客户也不能调用运营记录接口。 |
| 接口回归 | `verify-local-p0p1.ps1 -CreateActivityClosure` 以本机 QA 媒体邀请任务验证：客户调用记录接口为 `403`；平台管理员提交空说明为 `400 MEDIA_INVITATION_NOTE_REQUIRED`；随后重新读取任务仍为 `PENDING` 且无邀请时间，未伪造已联系事实。四服务独立项目、任务、订单、计价和三角色权限回归同时通过。 |
| 构建、运行与部署边界 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（68 项、失败 0、错误 0、跳过 0）。当前 Docker 前后端按源码无缓存重建，健康接口返回数据库/结构 `UP`、版本 `21`、合同 `winpress-v4.2.4-20260729`；生产 Compose 静态边界检查通过，未启动生产容器。 |
| 桌面与移动页面 | 客户会话下的当前 Docker 任务页在 1440×900 与 390×844 无页面横向溢出、无 Vite 错误覆盖层、无破损图片。运行中的 `TasksView` 资源已包含“沟通说明”与事实说明占位文案；运营专用表单由服务端接口回归和生产构建覆盖，未以客户会话代替运营角色的人工视觉验收。 |

本节只证明本机演示中的执行留痕、接口权限和当前页面基础可用性。它不证明媒体已被联系、媒体已回复、外部数据已授权、供应商已履约或生产环境已验收。

## 2026-07-29 P1 订单管理语义与跨端复测

| 检查 | 结果 |
|---|---|
| 四类订单分类 | 客户订单页保留云采写、媒体邀请、直编发稿、新闻发布会四个独立筛选项；本机客户范围的订单记录继续按四项服务分别展示，不合并为组合订单。 |
| 价格与交易边界 | 页面标题收敛为“服务订单记录”，不再把未形成付款或退款凭据的服务状态写成“交易记录”。未报价的媒体邀请、新闻发布会及待选渠道项目显示一次“待项目核验”；直编有当前计划价时才附加该核验提示。 |
| 结算语义 | “已确认的结算记录”改为中性的“结算记录”；待处理结算不再把 `0` 显示为已付金额。付款、退款、开票与对账仅在双方确认并形成凭据后进入结算记录，系统不以本机示例数据虚构交易事实。 |
| 接口与权限 | `verify-local-p0p1.ps1` 非写入式回归通过：健康与数据库结构为 `UP` / `21`，客户订单、结算、任务、项目字段白名单与跨客户拒绝继续通过；客户与发布运营均不能访问成本价、供应商或供应商订单接口。 |
| 构建、运行与页面 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（68 项、失败 0、错误 0、跳过 0）。本机 Docker 前端按源码无缓存重建。客户会话在 1440×900 与 390×844 的订单管理页均无页面横向溢出、无 Vite 错误覆盖层、无破损图片；四类筛选项与新文案均已加载。 |

本节只校正客户侧的订单与结算表述及其展示边界。它不新增在线支付、退款、发票、对账、供应商履约或外部交易能力；这些事项仍须凭正式合同、凭据和生产验收确认。

## 2026-07-29 P0 法律信息待确认与客户文案复测

| 检查 | 结果 |
|---|---|
| 法律文本状态 | 隐私页和服务条款页均增加首屏草案提示：运营主体、适用法律、保存期限、第三方处理、生产安全、收费/退款及争议条款待书面核验；页面不再把该内容表述为已生效的正式政策、合同或退款规则。 |
| 外部履约边界 | 服务边界页明确媒体目录、供应商、价格、排期与实际履约须经项目核验；本机演示不表示实时媒体资源、可约名单或实际履约。 |
| 客户文案 | 首页方法论阶段由“复盘”改为“结果”，只保留发布链接与客户项目记录的核验说明；API 评估清单去除“逐项确认”式内部过程表述。未增加支付、对账、外部数据、供应商或法律承诺。 |
| 前后端构建 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（8 个报告、68 项测试、失败 0、错误 0、跳过 0）。 |
| 本机部署与数据/权限回归 | 最终前端镜像已重建；前端、后端、PostgreSQL、Redis 均健康。健康接口返回数据库/结构 `UP`、版本 `21`、合同 `winpress-v4.2.4-20260729`。最终镜像上的无写入 `verify-local-p0p1.ps1` 通过客户/运营/管理员权限、跨客户拒绝、客户字段投影、附件公开路径阻断、后台成本/供应商隔离与未授权媒体不可用回归；此前同一源码基线的 `-CreateActivityClosure` 仍覆盖四项独立服务闭环。 |
| 生产编排边界 | `verify-production-compose-boundaries.ps1` 通过：生产 Compose 不会自动加载本机演示夹具或未验收媒体目录；生产容器未启动。 |
| 桌面页面 | 本机运行中的隐私、条款、服务边界和首页在 1265px 视口实测：新文案已加载、无页面横向溢出、无 Vite 错误覆盖层、无破损图片。 |
| 移动端验证边界 | 本轮仅改文案，响应式结构未变；`TrustPageView` 的 `max-width: 820px` 单列规则仍在。当前受控浏览器不提供移动视口模拟，嵌入式模拟亦被安全策略拒绝，因此不将历史 390px 截图冒充本轮实测。商用前需在真实 390px—430px 设备或受控预发布浏览器补做法律与服务边界页人工复核。 |

本节收紧的是法律、外部履约和客户文案的事实边界；它不完成经营主体、法务文本、外部数据授权、供应商合同、生产凭据或生产发布验收。

## 2026-07-29 P1 任务记录台账总数与加载态复测

| 检查 | 结果 |
|---|---|
| 问题修复 | 客户“任务记录”页原先从当前分页记录计算“进行中”数量，无法代表全部历史任务。现改用 `/task-records` 的全量 `total` 并显示为“任务记录”；加载时显示“正在汇总…”，不以初始 `0` 作为业务事实。 |
| 对客文案 | 项目台账改为“已核验成果”，页面不再使用“结果沉淀”等内部汇报式表述；未增加任何成果、媒体或履约承诺。 |
| 前后端构建 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（68 项、失败 0、错误 0、跳过 0）。 |
| 本机接口与权限 | 更新后的 Docker 前端容器已重建；`verify-local-p0p1.ps1` 非写入式回归通过健康、结构、客户字段白名单、三角色权限、跨客户拒绝、成本/供应商后台隔离和未授权媒体不可用。 |
| 桌面页面 | 客户会话在 1265px 视口实测任务台账显示的总数与接口返回 `total` 一致，无横向溢出、Vite 错误覆盖层或破损图片。 |
| 移动端验证边界 | 本轮只改总数取值与短文案，响应式结构未变。当前受控浏览器不提供可用移动视口，且不采用绕过方式伪造截图；390px—430px 真实设备或预发布浏览器复核仍为商用前待确认项。 |

本节不改变四项独立服务、客户权限、真实媒体资源、供应商履约、法律文本或生产凭据的验收状态。外部能力仍按待确认处理。

## 2026-07-29 P1 发布会选题资料上传闭环复测

| 检查 | 结果 |
|---|---|
| 下单页功能 | 发布会表单在“媒体与传播目标（选填）”下增加实际的“上传选题资料”控件。客户可在项目创建前选择最多 5 份资料；支持格式与单个 20MB 限制在页面明确展示。 |
| 资料关联 | 前端只在获得新建项目编号后逐份提交到受认证的 `/files` 项目接口；项目创建成功不因单份资料或名单保存失败而被回滚或伪装为全部成功，客户会被引导至项目详情补充。 |
| 实际上传回归 | `verify-local-p0p1.ps1 -CreateActivityClosure` 通过：最小三项资料的发布会项目可以上传明确标记的本地 QA 文本；读取项目详情时仅见许可文件元数据；第二客户向同一项目上传返回 `403`。 |
| 数据边界 | 文件公开路径仍为 `404`，匿名下载为 `401`；客户文件投影不含存储路径、校验值、上传人、成本、供应商或上游字段。上传材料不改变媒体资料“暂不可用”、人工补充待核验或外部履约待确认的状态。 |
| 构建与运行 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；`backend\\mvnw.cmd -q test` 通过（68 项、失败 0、错误 0、跳过 0）。更新后的前端 Docker 容器已重建；全链路闭环回归通过。 |
| 桌面页面 | 1265px 客户会话实测：上传区显示“选择资料”、支持格式、20MB 与 5 份限制；无横向溢出、Vite 错误覆盖层或破损图片。 |
| 移动端验证边界 | 本轮新增上传控件，不能用此前截图替代移动端实测。当前受控浏览器不提供可用移动视口且不采用绕过方式；390px—430px 的真实设备或预发布浏览器复核仍为商用前待确认项。 |

本节只证明本机演示中的文件受理、项目范围与客户字段边界；不证明生产存储、备份、病毒扫描、外部媒体推荐、供应商履约或生产凭据已验收。

## 2026-07-29 P1 看板权限口径与计划确认状态复测

| 检查 | 结果 |
|---|---|
| 看板统计修复 | 发布运营角色的进行中项目与发布会项目不再只按负责人统计；`/dashboard` 与其跳转的 `/projects`、`/publish-tasks`、`/writing-assignments` 复用同一权限范围。客户与发布运营的全部看板数字均已逐项和对应列表总数比对。 |
| 计划确认语义 | `POST /publish-plans/{planId}/confirm` 现返回 `CONFIRMED` 和客户公开任务号；确认仅建立项目任务，不把项目或稿件写成 `PUBLISHING`，不声称已向媒体或渠道提交。 |
| 四服务本机闭环 | 使用 `verify-local-p0p1.ps1 -CreateActivityClosure` 在隔离本机演示库建立明确标识的发布会根项目及云采写、媒体邀请、直编发稿三个独立项目。四类服务均形成各自任务记录和订单记录，活动关联不合并订单。该记录不代表真实客户、媒体或供应商交易。 |
| 计价与事实边界 | 云采写一名写手一天的订单金额为 980 元；直编发稿只记录所选客户服务价；新闻发布会和人工媒体邀请在未核验报价时保持无金额。媒体邀请任务不会被写成已联系、已发送或已拒绝。 |
| 无写入权限回归 | `verify-local-p0p1.ps1` 通过健康、结构、三角色会话、跨客户拒绝、客户字段白名单、成本/供应商后台隔离、未授权媒体不可用，以及客户与发布运营两类看板数字和列表总数的一致性检查。 |
| 构建与部署边界 | `frontend\\npm.cmd run build` 通过（1,732 个模块）；后端 Maven 测试通过（68 项、失败 0、错误 0、跳过 0）。本机健康接口为数据库/结构 `UP`、版本 `21`、合同 `winpress-v4.2.5-20260729`；本机 Docker 演示编排的前端、后端、PostgreSQL 和 Redis 均为健康状态。`verify-production-compose-boundaries.ps1` 通过，确认生产编排不会自动加载演示夹具或未验收媒体目录；未启动生产容器。 |
| 桌面页面 | 在 1280×720 视口检查本机首页：主标题、四项服务入口、客户可见的待核验提示和页脚法律待确认提示均可见；`scrollWidth = clientWidth = 1265`，无破损图片、Vite 错误覆盖层或控制台 warning/error。 |
| 移动端验证边界 | 本轮未以嵌入式或模拟视口替代真实移动端验收。390px—430px 真实设备或受控预发布浏览器的复核仍为商用前待确认项，不能把历史截图写为本次更新后的移动验收。 |

本节证明的是本机演示环境中的项目、权限和状态语义；不证明外部媒体数据已授权、供应商可履约、价格与排期已确认、法律信息已核验或生产部署已完成。

## 2026-07-29 P1 角色导航、版式与受理任务完整性复测

| 检查 | 结果 |
|---|---|
| 受理任务标题 | 新增 `22-service-intake-title-integrity.sql`。本机存量 4 条问号占位标题已按既有服务类型修复，迁移增加非空且不得全为问号的数据库约束。`verify-clean-local-db.ps1` 重建结果为 4 条受理任务、0 条占位标题；不新增媒体、供应商、成本或外部履约数据。 |
| 当前健康状态 | Docker `/api/v1/health` 返回 HTTP 200，数据库与结构均为 `UP`，结构版本 `22`，合同 `winpress-v4.2.6-20260729`。 |
| 角色导航 | 客户保留 PRD 规定的十项操作台入口；服务运营的执行工作以“当前待办”在前；平台运营将任务记录纳入项目运营，渠道与交易、平台管理分组清晰。三类角色的账号信息、当前账号与退出入口均在侧栏底部同一区域。 |
| 页面布局 | 桌面操作台移除与侧栏重复的顶部 Logo，不移除网站频道导航；看板去除重复 CTA；云采写固定价只出现一次；空状态具备说明与下一步入口；账号权限表的状态重复标签和列挤压已移除；操作日志当前动作与对象类型显示为中文业务名称。 |
| 桌面角色回归 | 受控本机浏览器在 1265px 视口下以客户、服务运营、平台运营三种实际会话逐页访问 29 个受保护页面。每页均有“首页、解决方案、行业资讯、方法论、案例展示、关于我们”频道导航，且 `scrollWidth` 未超过 `clientWidth`，无问号占位标题或页面级表单错误。关键截图保存在 `.qa/ui-audit-20260729/`。 |
| 前后端构建 | `frontend\\npm.cmd run build` 通过（1,733 个模块）；`backend\\mvnw.cmd -q test` 通过（68 项、失败 0、错误 0、跳过 0）。 |
| 数据库、权限与部署 | `verify-clean-local-db.ps1`、`verify-cold-start.ps1`、`verify-local-p0p1.ps1` 与 `verify-production-compose-boundaries.ps1` 均通过。后者只做生产编排静态边界检查，生产容器未启动。 |
| 移动端证据边界 | 本轮浏览器仅提供桌面视口；没有把旧移动截图作为本次导航和表格调整后的验收。390px—430px 真实设备或受控预发布浏览器复核仍为商用前待确认项。 |

本节证明本机演示环境中的导航、版式、数据完整性和权限回归；不证明外部媒体数据已授权、供应商可履约、价格与排期已确认、法律信息已核验或生产部署已完成。

## 2026-07-29 P0 本机数据库备份恢复复测

| 检查 | 结果 |
|---|---|
| 来源保护 | `verify-local-database-backup-restore.ps1` 只接受当前项目 `docker-compose.local-demo.yml` 启动且健康的 PostgreSQL 容器；Compose 文件与工作目录不匹配时拒绝执行，不连接生产或其他项目数据库。 |
| 隔离恢复 | 使用 PostgreSQL 自定义格式生成临时备份，恢复容器不映射宿主机端口、不使用持久化数据卷；脚本结束时临时容器、容器内备份和宿主机暂存目录全部清理。 |
| 恢复一致性 | 最终脚本复测的临时备份为 2,707,758 字节，SHA-256 为 `7e33bdaf978defa976298bfcc710a2bfd46efa825f46f5fbf96a013a6e612a59`。恢复后 41 张公共业务表及关键任务、项目、订单和附件元数据汇总与来源一致；四项独立服务类型保留，服务受理问号标题为 0，迁移 `22` 的约束存在。 |
| 数据边界 | 脚本只比较汇总，不输出客户正文、账号密码、令牌或业务明细。`file_asset` 元数据随数据库恢复，但上传文件本体、Redis、外部存储、生产加密与异地副本不在本次范围。 |
| 构建与自动化 | `frontend\\npm.cmd run build` 通过（1,733 个模块）；`backend\\mvnw.cmd -q test` 通过（68 项、失败 0、错误 0、跳过 0）；`verify-clean-local-db.ps1` 与 `verify-cold-start.ps1` 通过。 |
| 接口与权限 | `verify-local-p0p1.ps1` 通过健康、结构、客户/运营/管理员权限、跨客户拒绝、客户字段投影、成本/供应商后台隔离及未授权媒体不可用；当前合同 `winpress-v4.2.6-20260729`、结构版本 `22`。 |
| 生产隔离 | `verify-production-compose-boundaries.ps1` 通过且没有启动生产容器。本轮本机演示恢复不替代生产迁移或生产恢复；本记录后续章节另行完成生产等价非空成对恢复，真实恢复时限、TLS、域名和监控仍未验收。 |
| 页面 | 1280×720 本机首页实测可加载，`scrollWidth = clientWidth = 1265`，无破损图片、Vite 错误覆盖层、问号占位或可见错误提示。本轮未改 Vue 页面；当前受控浏览器没有新的真实移动视口，本轮不沿用历史移动截图。 |

本节把“本机演示数据库备份文件是否可恢复”从假设改为可重复验证，并未包装成生产方案。上传文件本体由下项验证；生产等价非空成对恢复见本记录后续章节，真实生产的加密、异地副本、RPO/RTO 与灾备切换仍是上线前阻断项。

## 2026-07-29 P0 本机上传文件备份恢复复测

| 检查 | 结果 |
|---|---|
| 来源保护 | `verify-local-upload-backup-restore.ps1` 从健康本机后端的 `/app/storage/uploads` 挂载反查唯一上传卷，并核对当前项目 Compose 文件、工作目录和卷标签；生产或其他项目来源被拒绝。 |
| 隔离恢复 | 上传卷先生成临时 `tar.gz`，再恢复到没有应用挂载、没有宿主机暴露的临时 Docker 卷。脚本不打印文件名或内容，只在临时目录内生成哈希与属性清单用于比对。 |
| 一致性 | 最终归档 373 字节，SHA-256 为 `fe2987f68cc99a7fe01f9a236687dd347741114f8da04fe83372e93adb247e1d`。4 个文件、2 个目录、368 字节文件本体恢复后，哈希、数字所有者、权限、大小和目录集合均与来源一致。计数只代表本机 QA 卷，不代表客户资料或生产规模。 |
| 失败路径 | 首次内容比对通过后，辅助容器因 `--rm` 已删除，清理代码重复删除导致退出非零，并留下 1 个临时卷和 1 个暂存目录。精确清理后改为存在性判断；最终脚本 `PASS`，临时卷、辅助容器、暂存目录均为 0。 |
| 数据边界 | 文件内容只用于本机哈希校验，不进入控制台、文档或源码；客户文件名、文件正文、账号、令牌和业务字段均未输出。 |
| 未覆盖范围 | 病毒扫描、对象存储、加密与密钥托管、异地副本、保留周期、RTO/RPO 和生产灾备切换均未验收；后续生产等价恢复只覆盖权威 PostgreSQL 数据与附件，Redis 会话和短期保护状态不作为业务台账。 |

## 2026-07-29 P0 当前源码包复测

| 检查 | 结果 |
|---|---|
| 部署模板 | 原规则误删 `.env.example`，导致归档缺少建立生产配置所需的无凭据模板。现仅放行根目录与前端 `.env.example`，真实 `.env` 和其他环境文件继续排除；模板中的密码项均为占位值，第三方令牌为空。 |
| 运行产物 | 原规则漏排 `.qa/`、`docs/qa/`、`docs/qa-render-*`、`BOOT-INF/` 和 `tmpWeb/`，会把页面截图、文档渲染中间件和 JAR 解包产物带入源码包。上述目录连同 `META-INF/` 已加入排除清单；最终 DOCX、Markdown 与 YAML 仍保留。 |
| 最终归档 | 由 `package-current-source.ps1` 在本记录更新后重新生成；实际文件名、大小和 SHA-256 以包外同名 `.sha256` 及交付链接为准。包内不回写自身校验值，避免自引用。 |
| 必需内容 | 最终归档共 234 个条目；两份环境模板、前后端项目文件、数据库结构、生产/本机 Compose、恢复脚本、V4.2 PRD、实际 GEO 联邦接口文档、审查与 QA 记录齐备，缺失数 0。 |
| 禁止内容 | 真实环境文件、令牌、测试账号说明、上传材料、日志、构建产物、`.qa` 截图、JAR 解包目录、旧归档、历史全量 SQL 快照及包内校验文件均未发现，禁止项 0。 |
| 中间产物 | 本轮生成的不合格或已被包内文档更新取代的中间归档及其校验文件已精确删除；最终归档保留，旧历史归档未被批量删除。被删除文件可由打包脚本重新生成，但不应交付。 |

归档检查证明当前源码包与交付边界一致，不证明生产凭据、外部媒体/供应商、法律文本或生产发布已经验收。

## 仍为待确认，不能对外表述为已具备

1. 牛媒及其他外部媒体数据：正式授权、数据范围、令牌、限流策略和联调验收均未作为本轮验收输入。当前只能显示“暂不可用”及人工补充路径，不能声称实时媒体/记者数据已接通。
2. 外部供应商联动：真实下单、报价、回执、对账、异常重试和服务等级需逐家合同、字段映射、沙箱与验收记录。平台内部管理能力不等于已接入真实履约。
3. 法律与商用信任信息：公司主体、备案、地址、客服、隐私与服务条款、案例授权及生产域名需要权利主体核验后再发布。
4. 生产运维：本记录后续已完成本机隔离的生产等价冷启动及非空 PostgreSQL/附件成对恢复；真实生产的备份加密、异地副本、保留周期、RPO/RTO、TLS/域名切换、监控告警和迁移演练仍未完成。
5. 历史组合服务：存量 `WRITING_AND_PUBLISHING` 记录需要业务确认后映射、迁移或归档；新链路不再创建组合下单。

## 上线前必须补齐

- 在隔离的生产预发布环境执行迁移记录、备份恢复、最小权限网络、TLS、域名和监控告警验证。
- 取得外部数据与供应商的书面授权后，再完成真实检索、下单、回执、超时和异常处理联调，并保留验收证据。
- 由主体方确认并发布法律文本、联系方式、案例授权及生产凭据；本机测试账号、演示数据和种子数据不得带入生产。
- 对历史组合记录形成映射清单，先经业务确认再做数据迁移，禁止自动拆单或删除。

## 2026-07-29 P0/P1 GEO 联邦接口与角色页面终检

| 检查 | 结果 |
|---|---|
| 实际接口合同 | GEO 文档已从未实现的公开 API 设想收敛为当前源码中的三个签名联邦入口：`POST /api/v1/integrations/geo/orders`、`POST /api/v1/integrations/geo/quotes`、`POST /api/v1/integrations/geo/catalog/direct-publishing-offers`。不存在的 `/openapi/v1`、`X-API-Key`、媒体搜索、文件上传和结果查询被明确列为未实现。 |
| 安全与限流 | 联邦入口增加 Redis 共享固定窗口限流及本机回退，默认每来源每分钟 120 次；仅使用来源摘要作为键。单元测试覆盖超限、分钟重置和 4,096 个本机来源上限。生产联邦和迁移均默认关闭，共享密钥为空。 |
| 运行时拒绝 | 本机 `POST /api/v1/integrations/geo/quotes` 在联邦关闭时返回 HTTP `503` 与 `FEDERATION_UNAVAILABLE`，未绕过签名或把待接入能力伪装为可用。 |
| 数据标识 | 平台看板和当前待办不再把普通商务咨询标成牛媒工单；新标签为“待处理咨询 / 商务咨询”，路由仍指向受权限保护的商务咨询后台。外部订单来源未完成生产验收前不显示伪造来源计数。 |
| 页面布局 | 以客户、服务运营、平台运营三种实际会话访问 27 个核心角色路由。侧栏菜单、分组、顺序和页面标题对应一致，页面级 `scrollWidth` 未超过 `clientWidth`。媒体与发布任务表去掉重复列后，管理员桌面表格为 7 列且 `scrollWidth = clientWidth = 937px`。 |
| 前后端构建 | `frontend\\npm.cmd run build` 通过（1,735 个模块）；`backend\\mvnw.cmd test` 通过（71 项、失败 0、错误 0、跳过 0）。 |
| 数据库、接口与权限 | 最终本机镜像上 `verify-local-p0p1.ps1` 通过结构版本 `22`、合同 `winpress-v4.2.6-20260729`、三角色会话、跨客户拒绝、客户字段白名单、成本/供应商后台隔离、未授权媒体不可用和看板/列表口径一致性。 |
| 部署边界 | 前端、后端、PostgreSQL、Redis 四个本机容器均健康。`verify-production-compose-boundaries.ps1` 通过，生产编排不会自动开启联邦迁移、预置共享密钥或载入演示数据；生产容器未启动。 |
| 文档 QA | Markdown、OpenAPI YAML 与 DOCX 已同步到当前实现；DOCX 经 LibreOffice 转 PDF 后逐页检查，共 7 页，无正文截断或孤立尾页。当前环境缺少 PyYAML，因此没有把 YAML 的官方 PyYAML 校验写成已通过。 |
| 移动端边界 | 当前受控浏览器没有可用的真实移动视口。本轮只确认响应式规则进入生产构建，不把历史截图、桌面缩放或模拟画布当作 390px—430px 实测。 |

本节证明的是当前本机联邦接口边界、三角色页面和权限回归。外部媒体数据许可、真实牛媒/供应商联调、法律信息和生产凭据仍是上线前待确认项。

## 2026-07-29 P1 三角色界面与移动端复测

| 检查 | 当前结果 |
|---|---|
| 客户导航 | “项目总览”包含传播看板、项目管理；“服务下单”包含提交需求、云采写、邀请媒体、直编发稿、新闻发布会；“任务与订单”包含待办事项、媒体与发布任务、任务记录、订单管理。菜单当前项与页面主标题一致。 |
| 服务运营导航 | 工作总览、服务执行、历史记录三组职责清楚；项目协同、当前待办、云采写任务、媒体与发布任务、任务记录均能进入对应页面。 |
| 平台运营导航 | 运营总览、服务执行、渠道与交易、平台管理四组职责清楚；渠道、报价、供应商订单、结算、咨询、权限和日志不进入客户菜单。 |
| 旧入口与独立服务 | `/channels` 重定向至独立直编发稿页面；旧的媒体邀请/直编发稿切换卡已删除。四项服务的独立下单入口、任务和订单页面没有重新合并。 |
| 下单表单 | 新闻发布会只有需求标题、会务联系人、联系电话三项必填；星号位于标签末尾，其余字段明确标注选填。现场采写的必要日期与地点仍为必填。 |
| 卡片工整性 | 1440×900 下三类看板主指标卡高度均为 104px，四项服务卡均为 96px，两张看板内容面板等高；菜单与标题无错位。 |
| 移动端 | 390×844 下客户、服务运营、平台运营受保护页面和 10 个公开页面均无页面级横向溢出；宽表只在表格容器内滚动。表格输入与下拉控件为 44px，初始直编发稿筛选不再被固定提交栏遮挡。 |
| 浏览器与构建 | 浏览器错误日志为 0；`npm run check` 通过类型与格式检查；Vue 生产构建通过（1,735 个模块）。 |
| 后端与运行 | Surefire 76 项通过（失败 0、错误 0）；前端、后端、PostgreSQL、Redis 四个本机容器健康。 |
| 生产预检 | 联邦启用时新增共享密钥、发行方、实例标识、公开 HTTPS 回调、超时、限流和布尔配置校验；合规合成配置通过，默认实例标识按预期被拒绝。生产 Compose 未启动。 |

此前记录中“当前浏览器没有可用移动视口”的结论只适用于当时工具状态，已由本节的明确 390×844 复测证据取代。外部媒体数据、供应商真实履约、法律信息和生产凭据仍未验收。

## 2026-07-29 P1 角色导航、页面密度与交易台账复核

| 检查 | 当前结果 |
|---|---|
| 角色导航 | 客户仍按“项目总览、服务下单、任务与订单”分组；服务运营按“工作总览、服务执行、历史记录”分组；平台运营按“运营总览、服务执行、渠道与交易、平台管理”分组。平台菜单“结算管理”同步为“结算与交易”，与页面主标题一致。账号信息均固定在侧栏底部并紧邻当前账号。 |
| 页面逐检 | 以三种实际登录会话检查客户 12 个、服务运营 7 个、平台运营 14 个核心路由；另检查首页、关于、API、联系、法律、隐私、条款、服务边界和案例标准 9 个公开页面。当前桌面视口均无页面级横向溢出，宽表只在自身容器滚动，公开页面顶部导航完整，破损图片为 0。 |
| 下单秩序 | 云采写与新闻发布会独立页面的业务介绍卡补齐第 1 步标识，后续项目信息、媒体筛选和需求说明连续显示 1—4，不再从第 2 步突兀开始。云采写页面主标题与侧栏名称统一为“云采写”。 |
| 页面密度 | 平台采写派单改为每页 10 条，操作日志改为每页 20 条，历史数据不再把单页拉长到 8,000px 或 4,900px。客户订单的章节标题、说明、筛选与表格之间补齐统一内边距。 |
| 交易语义 | 新增可追溯交易台账后，客户只查看交易编号、类型、项目、金额、时间、凭据或客户说明及状态；平台运营可登记、作废并保留内部审计信息。结算单状态不再以手工改为“已支付”伪造付款，当前菜单与页面统一称“结算与交易”。 |
| 构建与浏览器 | `npm.cmd run check` 通过；Docker 内 Vue/TypeScript/Vite 生产构建通过（1,736 个模块）。本机前端重新构建后健康，HTTP 200；浏览器控制台错误与警告记录为 0。 |
| 后端与数据库 | 本轮同一基线的 Java Surefire 80 项通过，结构版本为 `23`，接口合同为 `winpress-v4.2.7-20260729`；本机后端、PostgreSQL、Redis 均保持健康。生产 Compose 未启动。 |
| 跨端边界 | 本轮改动只增加可换行的步骤标识、面板间距与分页，并继续使用既有 760px/420px 响应式规则和 44px 触控规则。此前同一版本线的 390×844 回归仍作为基线；本次受控浏览器未重新提供可切换的真实移动视口，因此不把桌面裁剪当作新的移动截图证据。 |

本节证明当前本机界面、交易台账和角色边界可运行、可追溯；不证明外部媒体目录、供应商履约、法律信息或生产凭据已经验收。

## 2026-07-29 P1 已保存发布计划的确认闭环

| 检查 | 结果 |
|---|---|
| 持久入口 | 媒体邀请与直编发稿的已保存计划，会在项目详情的“发布计划／计划确认”区持续展示；客户不再必须保持原下单弹窗打开。 |
| 待确认队列 | 客户可通过 `/work-items?scope=planConfirmation` 只查看状态为 `WAITING_CONFIRMATION` 的已保存计划；传播看板新增“待客户确认”指标，指标计数与该队列使用同一查询范围复核一致。 |
| 对客语义 | 客户点击“提交项目核验”后，计划进入项目核验并建立后续任务；页面明确说明不会自动向媒体发出邀请或向渠道提交稿件，不把未验收外部动作伪装为已经发生。 |
| 字段边界 | 该客户队列及项目计划列表仅投影计划公开编号、项目、标题、状态、金额/待核验说明、条目数与时间；内部记录主键、供应商、成本、上游标识、操作人和内部规则均不返回。 |
| 构建、接口与权限 | `npm.cmd run check`、`npm.cmd run build` 均通过（生产构建 1,737 个模块）；Java Surefire 81 项通过，失败/错误/跳过均为 0；`verify-local-p0p1.ps1` 完整通过，包含客户确认队列字段边界、看板与列表口径一致性、跨客户拒绝及三角色权限。 |
| 本机运行与页面 | 本机前端、后端、PostgreSQL、Redis 均为 healthy；前端 HTTP 200，健康接口返回结构 `23`、合同 `winpress-v4.2.7-20260729`。1265px 桌面视口实测项目详情“计划确认”区正常换行、无页面级横向溢出。 |

本节只证明本机演示环境中的计划确认入口、权限投影与状态口径。外部媒体数据授权、供应商实单、法律信息、生产凭据与生产发布仍为待确认项；本轮未把桌面截图替代新的移动端验收。

## 2026-07-29 P1 四项服务待平台执行队列

| 检查 | 结果 |
|---|---|
| 复核发现 | 传播看板原“待平台执行”数字只统计媒体邀请与直编发稿的发布任务，未覆盖云采写派单、两类服务受理任务和新闻发布会执行清单，指标名称、数字和目标列表不一致。 |
| 统一口径 | 看板指标 `pendingPlatformExecutions` 与 `/task-records?scope=pendingExecution` 复用同一四服务联合查询。云采写纳入待匹配、已发出邀约、已接单和待重新匹配；媒体邀请与直编发稿纳入待受理、待分配、待执行、执行中和异常；新闻发布会纳入待处理、进行中和受阻。客户待补资料、客户确认、已完成、已验收和已取消记录不进入该队列。 |
| 稳定标识 | 任务记录新增客户可见的 `serviceType` 四服务类型码，页面继续显示中文服务名。自动化不再依赖可能受编码或文案调整影响的中文标签；未知 `scope` 返回 HTTP 400。 |
| 客户边界 | 客户任务记录只投影公开任务编号、项目、任务标题、四服务类型、服务名称、状态、计划/完成时间、客户可见说明和更新时间；内部任务类型与主键、负责人、供应商、成本、上游标识和内部规则均不返回。 |
| 三角色回归 | 客户、服务运营和平台运营的“待平台执行”数字均与其权限范围内目标列表总数相等；跨客户访问、客户成本/供应商隔离、未授权媒体不可用及未知筛选拒绝均通过。 |
| 构建与测试 | `npm.cmd run check`、`npm.cmd run build` 通过，Vue 生产构建转换 1,737 个模块；Java Surefire 83 项通过，失败、错误、跳过均为 0；`verify-local-p0p1.ps1` 完整通过。 |
| 页面与运行 | 1440×900 桌面端从传播看板唯一跳转至待平台执行列表，首屏可见四类任务；页面级 `scrollWidth` 与 `clientWidth` 一致。390×844 受控视口下页面无整页横向溢出，任务宽表在 358px 容器内独立横向滚动；控制台错误和警告为 0。前端、后端、PostgreSQL、Redis 四个本机容器均 healthy，前端 HTTP 200，结构版本 `23`、接口合同 `winpress-v4.2.7-20260729`。 |

本节证明本机演示环境内四项服务的统一执行队列、角色范围和客户字段边界；不证明外部媒体数据、供应商实单、法律信息或生产凭据已经验收。

## 2026-07-29 P1 四项服务稳定订单主记录

| 检查 | 结果 |
|---|---|
| 复核发现 | 原订单页把采写派单、发布任务和发布会项目临时拼成客户订单。存量数据中，56 个直编发稿项目被展开为 59 条订单记录，38 个媒体邀请项目被展开为 39 条；41 个云采写项目因 3 个项目尚未派单而只剩 38 条。任务拆分、重试或缺少派单会改变客户订单数量和编号，不符合“一次独立下单保留一条订单记录”。 |
| 稳定主记录 | 客户订单现以四类 `customer_requirement + project` 为主记录，每个独立服务项目固定对应一条订单，公开订单号沿用最初的需求编号。后续派单、发布计划、渠道任务和发布会进度只更新订单状态、服务摘要和计划金额，不再生成或替换客户订单。 |
| 任务与供应商边界 | 客户服务订单、执行任务、结算交易和供应商采购订单继续分账管理。供应商、成本价、上游标识、内部主键、操作人和内部规则不进入客户订单接口或页面。 |
| 计价语义 | 云采写按已记录的 `980 元/人天 × 天数 × 写手数` 展示；直编发稿只在已有客户计划价时显示该价格并标注待项目核验；人工媒体邀请与新闻发布会在未确认报价时保持“待项目核验”，不制造金额。 |
| 数据与权限回归 | 只读回归逐项比较四类 `/projects?serviceType=...` 与 `/order-records?serviceType=...` 总数，全部一一相等；客户字段白名单、跨客户拒绝、成本与供应商后台隔离均通过。客户看板“云采写”数量改为与其可见订单台账一致，服务运营仍按实际派单统计。 |
| 实际闭环 | 本机专用回归在同一活动下分别创建新闻发布会、云采写、媒体邀请和直编发稿。四项各形成一条且仅一条正确类型的订单，订单号均等于原需求号；云采写为 980 元，直编计划价与所选客户价一致，人工邀请和发布会均保持未报价。该场景不联系媒体、不提交外部渠道，也不创建真实供应商订单。 |
| 页面工整性 | 1440×900 客户订单表无需桌面横向拖动，整页 `scrollWidth = clientWidth`；长订单号、项目和服务内容受控换行，不再与相邻列叠压。390×844 下整页无横向溢出，订单表只在 343px 容器内滚动；两个筛选框和查询按钮均为 44px，移动侧栏可打开，浏览器错误与警告为 0。 |
| 构建与运行 | `npm.cmd run check` 通过；Vue/TypeScript/Vite 生产构建通过（1,737 个模块）；同一后端基线 Java Surefire 83 项通过。最终健康状态、结构版本、接口合同和源码包校验值以本轮交付前复测为准。 |

本节证明本机演示环境内“四项独立服务下单—项目—任务—订单”的稳定客户订单主记录。它不证明外部媒体目录、记者数据、供应商真实履约、法律信息、生产凭据或生产发布已经验收。

## 2026-07-29 P1 现行结算与历史组合记录分离

| 检查 | 结果 |
|---|---|
| 复核发现 | 本机旧版 `WRITING_AND_PUBLISHING` 组合项目仍有一条状态为 `PENDING` 的结算记录。项目详情已把该项目标为历史归档，但客户订单页仍把对应金额混入现行“结算记录”，平台后台也可尝试修改状态、发票和交易，造成旧模型像当前应付款项一样继续运行。 |
| 数据处理 | 未删除、改价、取消或推测历史记录，也未增加数据库迁移。旧组合服务结算通过独立的客户只读归档接口返回；四项现行服务继续使用原结算接口。历史数据仍可追溯，但不再计入当前待结账单。 |
| 后端边界 | 现行客户结算查询只接受 `ONSITE_WRITING`、`MEDIA_PR`、`DIRECT_PUBLISHING`、`NEWS_CONFERENCE` 四种服务；旧组合记录统一标记 `archiveOnly`。平台更新结算、登记交易和作废交易都会再次校验该标记，并以 `ARCHIVED_SETTLEMENT_READ_ONLY` 拒绝历史归档变更。 |
| 客户页面 | 订单管理页把“结算记录”和“历史结算归档”分开。归档表只使用“原记录金额、原记录已付、原到期时间、原记录状态、只读归档”等追溯语义，不把历史未结余额表述为当前待付款。 |
| 平台页面 | 结算与交易页显示服务类别和“只读归档”标记；历史记录的发票、状态和保存按钮禁用，交易弹窗只能查看历史。金额四项合并为紧凑分组，账务信息合并展示，1440×900 内容区的表格 `scrollWidth` 与 `clientWidth` 均为 1097px，状态与操作无需桌面横向拖动。 |
| 权限与接口回归 | `verify-local-p0p1.ps1` 完整通过：客户现行列表只含四项服务，客户归档列表只含历史记录，字段白名单无供应商、成本、运营人、上游标识或内部备注；发布运营访问两个客户结算接口均为 HTTP 403；平台修改归档结算为 HTTP 409。 |
| 构建与测试 | `npm.cmd run check`、`npm.cmd run build` 通过，Vue 生产构建转换 1,737 个模块；Java Surefire 86 项通过，失败、错误、跳过均为 0。 |
| 跨端与运行 | 客户订单页、客户历史归档和平台结算页在 1440×900、390×844 下均无页面级横向溢出；移动宽表只在自身容器滚动；浏览器错误日志为 0。本机前端、后端、PostgreSQL、Redis 四个容器健康，健康接口返回结构 `23`、合同 `winpress-v4.2.8-20260729`。 |
| 部署边界 | 本机与生产 Compose 使用受控模板完成静态解析，生产数据边界脚本通过；生产容器没有启动。外部媒体数据、供应商真实履约、法律信息和生产凭据仍未验收。 |

本节只修复当前账务与历史归档的产品边界，不认定任何历史记录已经付款、取消、开票或完成对账；需要处理真实历史债权债务时，必须另行取得凭据和人工审批。

## 2026-07-29 P1 现行交易与历史交易归档分离

| 检查 | 结果 |
|---|---|
| 复核发现 | 旧组合服务结算已经进入只读归档，但客户 `/transaction-records` 仍按组织和项目查询全部交易。若历史库曾登记收款、退款或调整，旧交易仍会混入当前四项服务交易台账，造成“结算单已归档、交易仍现行”的口径冲突。 |
| 接口分离 | `/transaction-records` 只返回 `ONSITE_WRITING`、`MEDIA_PR`、`DIRECT_PUBLISHING`、`NEWS_CONFERENCE` 四项服务；新增 `/transaction-archive-records` 专门返回旧组合服务的历史交易事实。两类记录均返回服务类别与 `archiveOnly`，但采用同一客户字段白名单。 |
| 数据与权限边界 | 两个客户交易接口均按当前用户、组织和项目归属过滤，不返回内部说明、登记人、作废人、作废原因、供应商、成本或上游标识；发布运营访问现行与归档交易接口均为 HTTP 403。平台仍可查历史事实，但归档交易不能新增或作废。 |
| 客户页面 | “交易记录”明确只显示当前四项服务；历史结算卡内增加“历史交易凭据”，使用“原交易编号、原记录金额、原记录状态、只读归档”等追溯语义。当前本机历史组合项目没有已登记交易，因此页面如实显示“暂无历史组合服务交易记录”，不制造示例收款。 |
| 自动化覆盖 | Java 单元测试分别验证现行与归档投影及内部字段排除；本机回归验证现行交易只含四服务、归档接口字段安全、发布运营无权访问、当前交易不引用归档结算编号，并在第二客户范围复核四服务类型。 |
| 构建与运行 | Vue 类型、格式与生产构建通过（1,737 个模块）；Java Surefire 87 项通过，失败、错误、跳过均为 0；本机完整 P0/P1 回归通过。四个本机容器健康，结构版本 `23`，接口合同 `winpress-v4.2.9-20260729`。 |
| 跨端实测 | 客户订单页在 1440×900 与 390×844 受控视口下均无页面级横向溢出、无破损图片、无应用控制台 warning/error；历史宽表仅在自身容器横向滚动，移动端可见交互目标没有小于 44px。窄屏归档装饰图标已隐藏，避免标题区产生空行。 |

本节只建立现行交易与历史交易的技术归档边界；不判断历史交易是否真实发生，也不据此确认付款、退款、开票、核销或债权债务。

## 2026-07-29 P0 生产等价空库冷启动与公开页终检

| 检查 | 结果 |
|---|---|
| 隔离策略 | 新增 `verify-production-cold-start.ps1`。脚本拒绝既有 `winpress-production-*` 容器，自动选择未占用端口，使用随机临时凭据、独立 Compose 项目、独立卷和独立质检镜像标签；失败或默认完成时自动清理。 |
| 真实冷启动 | 使用最终源码构建并启动 PostgreSQL、Redis、Spring Boot 与 Vue/Nginx 四个生产等价容器，四者均达到 `healthy`。健康接口返回结构 `23`、合同 `winpress-v4.2.9-20260729`、数据库与结构状态 `UP`。 |
| 生产空库 | `app_user`、组织、需求、项目、渠道、报价、供应商、供应商订单和商务咨询均为 `0`；只初始化 3 个角色、10 项权限和一条云采写现场服务公开价 `980 CNY / PERSON_DAY`。未载入测试账号、案例、演示订单或未验收媒体目录。 |
| 接口与端口边界 | 牛媒接口、GEO 联动及迁移保持关闭，共享密钥为空；`/files/` 和 `/test-accounts.html` 返回 `404`，媒体状态接口返回 `401`，禁用的 GEO 公共状态入口返回 `404`。四个宿主机端口仅绑定 `127.0.0.1`。 |
| 公开页面 | 最终生产前端在 1440×900 与 390×844 下均无页面级横向溢出、无破损图片、无“本机演示环境/选择测试身份”标记，顶部导航顺序为“首页、解决方案、行业资讯、方法论、案例展示、关于我们”，控制台 warning/error 为 `0`。 |
| 移动触控修复 | 首轮检查发现首页服务卡与页脚文字链接只有 16—19px 高；已将 540px 以下的卡片、正文入口与页脚链接触控区域统一扩展，最终 390×844 复测所有可见链接与按钮均不小于 40×40px，主要文字链接为 44px 高。 |
| 构建 | `npm.cmd run check` 通过类型与格式检查；`npm.cmd run build` 通过，Vite 转换 1,737 个模块；同一最终源码的 Java Surefire 87 项通过，失败、错误、跳过均为 `0`。 |
| 清理与回归 | 验收结束后隔离容器、数据卷、质检镜像、状态文件与临时凭据均为 `0`；原本机演示四容器继续 `healthy`，5217 首页 HTTP `200`，8192 健康接口继续返回结构 `23` 与合同 `v4.2.9`。 |

本节证明当前源码能够按生产编排从空库启动，并验证默认数据、接口和端口边界；它不是正式生产发布。后续章节已补齐首个平台管理员开通工具和非空数据库/附件成对恢复；正式身份与凭据托管、域名、TLS、监控、加密异地备份、网络策略、外部媒体/供应商授权、法律信息和真实履约仍须单独验收。

## 2026-07-29 P0 首个平台管理员受控开通

| 检查 | 结果 |
|---|---|
| 复核发现 | 生产等价空库能够健康启动，但公开注册只创建客户账号，后台账号管理又要求已有 `PLATFORM_ADMIN`。此前文档所称“受控管理员开通流程”没有实际工具，正式部署会停在“没有管理员、也无人有权创建管理员”的状态。 |
| 开通工具 | 新增 `bootstrap-production-admin.ps1`。它要求显式 `-ConfirmCreate`，只接受健康的正式生产 PostgreSQL Compose 目标；组织、人员、手机号、登录邮箱与密码均由部署负责人交互提供，不携带默认凭据。 |
| 一次性约束 | 数据库事务使用排他锁，要求结构、启用的 `PLATFORM_ADMIN` 角色及 10 项权限完整；只要存在任何历史平台管理员角色关系即拒绝再次执行。后续账号维护必须通过已认证管理接口，不能将开通工具用作账号恢复后门。 |
| 敏感信息 | 密码以隐藏 `SecureString` 输入，经标准输入传给数据库事务；脚本不打印联系方式、密码、编码值、数据库密码、令牌或密码哈希。审计只记录开通方式和公开组织编号，不保存邮箱、手机号、密码、令牌或密钥。 |
| 创建边界 | 成功后只有一个 `PLATFORM` 组织、一个启用的 `PLATFORM_ADMIN`、一条角色关系和一条 `BOOTSTRAP_PLATFORM_ADMIN` 审计；未创建客户、需求、项目、媒体、报价、供应商、供应商订单或商务咨询。密码哈希为 bcrypt cost 12。 |
| API 实测 | 在真实生产等价空库中，合成管理员通过 `/auth/login` 登录，`/auth/me` 返回 `PLATFORM_ADMIN` 与 10 项权限，受保护的 `/admin/users` 返回唯一账号；匿名访问返回 `401`，退出后原令牌再次访问也返回 `401`。 |
| 重复执行 | 使用另一组合成身份再次执行时返回 `ADMIN_BOOTSTRAP_ADMIN_EXISTS`。复核组织、用户、角色关系和开通审计仍各为一条，拒绝事务没有产生残留。 |
| 兼容性与清理 | 首轮验收发现无 BOM 的中文 PowerShell 提示在 Windows PowerShell 5.1 下解析失败；提示改为纯 ASCII 后完成同版本实测。最终隔离容器、卷、质检镜像、状态文件与临时凭据均已清理。 |
| 完整回归 | Windows PowerShell 5.1 静态解析错误为 `0`；前端类型/格式检查及生产构建通过（1,737 个模块）；Java Surefire 87 项通过，失败、错误、跳过均为 `0`；本机完整 P0/P1 权限与数据边界回归、生产 Compose 边界检查均通过。5217 首页 HTTP `200`，8192 健康接口返回结构 `23`、合同 `winpress-v4.2.9-20260729`，本机四容器均为 `healthy`。 |

本项只补齐正式部署后的首个权限入口，并证明工具可控、可拒绝、可审计。当前没有创建真实生产管理员；管理员真实身份、密码保管、多因素认证、生产域名、TLS、备份、监控、法律信息、外部媒体与供应商履约仍需上线前单独验收。

## 2026-07-29 P0 生产等价数据库与附件成对恢复

| 检查 | 结果 |
|---|---|
| 复核发现 | 既有生产等价冷启动只能证明空库可初始化；本机演示备份脚本又明确拒绝生产 Compose。此前没有证据证明生产镜像形成的非空项目数据库与附件卷能够成对备份、隔离恢复并保持引用一致。 |
| 来源隔离 | 新增 `verify-production-backup-restore.ps1`。脚本只能调用并接受 `winpress-production-cold-start`，逐一核验 PostgreSQL、后端和附件卷的 Compose 项目、服务、配置文件、运行与健康状态；真实生产或其他项目不能成为数据源。 |
| 非空业务样本 | 通过正式 API 注册一个 `.invalid` 合成客户，分别创建新闻发布会、云采写、媒体邀请和直编发稿四个独立项目并关联到同一活动；发布会仅使用标题、会务联系人和联系电话创建，确认生成 9 项项目清单；再通过受保护附件接口上传并下载一个合成文本文件。没有直接伪造客户项目、任务、订单或附件记录。 |
| 四服务闭环 | 客户任务台账可分别找到四个项目及对应服务类型；订单台账只有四条相关记录，公开订单号与各自原始需求号一致。云采写一人一天金额为 `980 CNY`，发布会、媒体邀请和未选渠道的直编发稿金额为空，活动关联没有合价或复制订单。 |
| 数据边界 | 备份前只有 1 个组织、1 个客户、4 个需求、4 个项目、1 项采写派单、2 项服务受理任务、1 个发布会项目、9 项发布会清单和 1 个活动附件；3 个角色、10 项权限和 `980 CNY / PERSON_DAY` 公开价保持完整。媒体目录、报价、发布计划、发布任务、供应商和供应商订单均为 `0`，牛媒与 GEO 未启用；客户项目、任务、订单和媒体状态响应不含供应商、成本、上游标识或令牌。 |
| 数据库备份恢复 | 使用 PostgreSQL custom archive，先读取归档目录，再恢复到 `--network none`、无宿主机端口、无持久化数据卷的临时 PostgreSQL 17 容器。源库与恢复库的关键表汇总、约束、角色权限、公开价、四类需求、四个项目、采写派单、服务受理任务、发布会清单和附件元数据逐项一致。 |
| 附件备份恢复 | 附件卷归档为 `tar.gz`，将目录集合、文件 SHA-256、数字所有者、权限、大小和总字节写入受保护临时清单；恢复到未挂载应用的临时卷后，各项清单完全一致。恢复库的附件路径、SHA-256 和大小与恢复文件交叉匹配。 |
| 故障与修复 | 首轮完整恢复后，`--rm` 助手容器自动删除与显式清理发生竞态，导致后续资源清理中断；按 Compose 标签核验后精确删除该批 QA 资源。清理改为幂等、逐项继续并在末尾统一检查残留，第二轮完整演练通过。 |
| 无残留 | 最终生产命名 QA 容器、冷启动卷、恢复卷、质检镜像、状态文件、备份、归档、临时凭据及执行日志均为 `0`；本机演示栈未被接管。 |
| 最终回归 | 四个生产控制脚本在 Windows PowerShell 5.1 下静态解析错误为 `0`；前端类型/格式检查和生产构建通过（1,737 个模块）；Java Surefire 87 项通过，失败、错误、跳过均为 `0`；本机完整 P0/P1 接口与权限回归、生产 Compose 边界检查均通过。5217 首页 HTTP `200`，8192 健康接口返回数据库 `UP`、结构 `23`、合同 `winpress-v4.2.9-20260729`，本机四容器均为 `healthy`。首页、客户传播看板和四服务下单页在 1440×900 与 390×844 实测无页面级横向溢出、破图、Vite 覆盖层或控制台告警/错误；移动操作菜单与频道导航均可独立打开和关闭，首屏可见控件没有小于 40×40px 的样本。 |

本项是当前源码在本机隔离条件下的生产等价非空恢复证据，不等于真实生产备份验收。正式环境的备份加密、密钥托管、异地副本、保留周期、RPO/RTO、调度、告警、访问审批和灾备切换仍须由生产责任人验证。Redis 只承载会话与短期保护状态，不在本次权威业务数据恢复范围内。

## 2026-07-29 P0 生产 Swagger 与 CORS 运行边界

| 检查 | 结果 |
|---|---|
| 复核发现 | 生产后端此前会匿名返回 Swagger 与 OpenAPI；CORS 代码还在部署配置之外固定放行 localhost、127.0.0.1 和 HTTP 域名，导致“预检只准 HTTPS”与实际运行不一致。 |
| 配置修复 | Springdoc 默认关闭；生产 Compose 固定 `WINPRESS_API_DOCS_ENABLED=false`，本机演示 Compose 单独固定为 `true`。生产预检要求环境文件显式为 `false`，Compose 边界脚本拒绝生产开启。 |
| CORS 修复 | 删除全部代码内置来源，只按 `WINPRESS_CORS_ORIGINS` 的明确、去重列表配置 `allowedOrigins`；空来源直接拒绝启动。生产预检继续拒绝 localhost 和非 HTTPS 来源。 |
| 生产运行验证 | 真实生产等价冷启动中 `/swagger-ui/index.html`、`/swagger-ui.html`、`/v3/api-docs` 均返回 `404`。已配置的合成 HTTPS 来源预检返回精确放行头；localhost 与 HTTP 正式域名均被拒绝且无放行头。 |
| 本机兼容 | 本机后端重新编译并重建；`/api/v1/health`、Swagger 和 OpenAPI 均返回 `200`，`http://127.0.0.1:5217` CORS 预检返回精确来源。前端、后端、PostgreSQL、Redis 四容器均为 `healthy`。 |
| 构建与权限 | Java Surefire 87 项通过，失败、错误、跳过均为 `0`；前端 `check` 与 1,737 模块生产构建通过；`verify-local-p0p1.ps1` 完整通过三角色、跨客户、四服务、订单、结算、交易与内部字段隔离；生产等价四服务非空数据库/附件成对恢复复跑通过。 |
| 跨端复核 | 首页、客户传播看板和下单页在 1440×900 与 390×844 下无整页横向溢出、破图或控制台告警；移动菜单可独立使用，可见控件不小于 40×40px。 |

本节只收紧内部文档与浏览器来源边界，不表示正式生产已部署，也不表示 GEO 对客接口、媒体数据或供应商联动已经完成授权与验收。

## 2026-07-29 P0 会话代次与文件提交原子性

| 检查 | 结果 |
|---|---|
| 账号权限变更 | 会话不再只依赖可能过期或缺失的用户令牌索引。账号状态或角色变更会轮换用户会话代次；受保护请求同时校验代次、用户状态、组织状态、唯一有效角色和当前权限。变更前令牌不能在账号恢复后重新生效。 |
| 索引缺失实测 | 本机专用脚本先删除一个隔离演示账号的 Redis 会话索引，再执行停用与恢复。旧令牌返回 `401`，新登录成功，账号恢复原角色和启用状态；账号、密码、令牌和用户标识均未输出。 |
| 登录边界 | 会话令牌只接受固定的 32 位小写十六进制格式；任意路径式或畸形令牌在访问 Redis 与数据库前即被拒绝。组织停用、账号停用或多个有效角色同时存在时登录/会话均失败关闭。 |
| 文件提交 | `file_asset` 与上传审计进入同一事务；数据库提交失败时物理文件自动清理。PDF、Office、图片与文本上传增加基础签名校验，改扩展名的伪 PDF 返回 `400 FILE_CONTENT_MISMATCH`，不形成文件元数据。 |
| 业务闭环 | `verify-local-p0p1.ps1 -CreateActivityClosure` 继续通过发布会最小三项资料、九项清单、合法附件、跨客户上传拒绝，以及云采写、媒体邀请、直编发稿、新闻发布会各自的项目、任务和稳定订单闭环。 |
| 自动测试 | Java Surefire 96 项通过，失败、错误、跳过均为 0；新增会话当前权限同步、账号失效、代次不匹配、索引缺失代次轮换、文件伪装拒绝和入库失败清理覆盖。Vue 类型、Prettier 与生产构建通过，转换 1,737 个模块。 |
| 数据与部署 | 干净数据库重建、本机数据库备份恢复、本机附件卷恢复、生产 Compose 边界、生产等价空库冷启动和四服务非空数据库/附件成对恢复均通过。结构 `23`、接口合同 `winpress-v4.2.10-20260729`；真实生产未启动。 |
| 页面复核 | 1440×900 与 390×844 首页均无页面级横向溢出、破图或应用控制台 warning/error；390px 首屏可见控件高度最小 44px。登录页顶部频道完整，“返回首页”按钮实测返回首页。 |

外部媒体与记者数据、供应商真实履约、法律信息、正式生产账号与凭据、域名、TLS、多因素认证、监控和正式灾备没有因本项自动变为已验收，继续保持待确认。

## 2026-07-29 P0 服务需求提交幂等性

| 检查 | 结果 |
|---|---|
| 产品基准 | 有效 PRD 已由产品负责人确认继续使用；四项服务独立下单、独立项目、任务与订单闭环，以及客户与内部数据边界均未改变。 |
| 风险定位 | 发布计划确认已有行锁与唯一约束；首次需求提交此前没有幂等键，双击、超时重试与并发请求可能生成重复项目和订单。 |
| 接口控制 | `POST /api/v1/requirements` 强制 `Idempotency-Key`。同客户、同组织、同键、同内容复用原项目；同键不同内容返回 `409 IDEMPOTENCY_KEY_REUSED`；缺少键返回 `400 IDEMPOTENCY_KEY_REQUIRED`。 |
| 并发控制 | 事务内以客户、组织和幂等键获取 PostgreSQL advisory lock，并在锁内复查；数据库结构 `24` 增加成对字段约束与客户、组织范围唯一部分索引。 |
| 客户端续提 | 页面防止按钮重复触发；仅保存请求指纹和随机键，不保存表单正文。网络失败或刷新后相同内容复用原键，成功后清理。 |
| 真实接口回归 | 两个同时发出的 HTTP 请求均返回 `200`，只产生一个项目编号；同键不同内容返回 `409`。四服务闭环、订单稳定性、跨客户权限、供应商与成本字段隔离全部继续通过。 |
| 自动测试与构建 | Java Surefire 98 项通过，失败、错误、跳过均为 `0`；Vue 类型、Prettier 与生产构建通过，转换 1,737 个模块；七个本轮修改的 PowerShell 验收脚本解析错误均为 `0`。 |
| 数据与部署 | 干净数据库、生产等价空库冷启动、本机数据库与附件恢复、四服务非空数据库/附件成对恢复及生产 Compose 边界全部通过。健康接口返回结构 `24`、合同 `winpress-v4.2.11-20260729`。 |
| 页面复核 | 云采写、媒体邀请、直编发稿、新闻发布会下单页在 1440×900 与 390×844 下无页面级横向溢出、破图、Vite 覆盖层或控制台告警。发布会三项必填规则保持不变。 |

本项证明当前源码在本机与生产等价隔离环境中的重复提交防护，不代表真实生产已上线。外部媒体与记者数据、供应商联动、法律信息、生产凭据、域名、TLS、监控和正式灾备继续标记为待确认。

## 2026-07-29 P0 客户验收终态与移动任务可读性

| 检查 | 结果 |
|---|---|
| 产品基准 | 有效 PRD 继续作为唯一验收基准；本项未新增组合服务，也未改变四项服务独立下单、项目、任务和订单规则。 |
| 验收前提 | 客户只能验收已有已核验成果的 `COMPLETED` 任务。缺少成果返回 `TASK_RESULT_REQUIRED`，任务状态不变。 |
| 终态保护 | `CLIENT_ACCEPTED` 为不可回退终态；运营更新返回 `409 TASK_FINALIZED`，再次回填成果返回 `409 TASK_ALREADY_ACCEPTED`。已完成任务重复成果返回 `TASK_RESULT_ALREADY_SUBMITTED`。 |
| 并发一致性 | 任务更新、媒体邀请状态、成果提交和客户验收均先锁定任务，再按预期状态条件更新；并发状态变化返回 `TASK_STATE_CHANGED`，不覆盖新事实。 |
| 成果校验 | 成果地址仅接受具有有效主机名且不含用户信息的 HTTP/HTTPS URL；明显晚于服务器当前时间的发布时间被拒绝。数据库禁止同一任务重复保存同一 URL。 |
| 真实接口闭环 | 本机实际完成直编任务开始、成果回填、客户验收与重复验收；随后回退和二次成果均被拒绝，客户任务保持已验收，项目只保留一条已核验成果。四服务项目、任务和稳定订单继续分别可查。 |
| 数据库与部署 | 结构 `25`、接口合同 `winpress-v4.2.12-20260729`。干净库、冷启动、本机数据库与附件恢复、生产 Compose 边界、生产等价空库和四服务非空成对恢复均通过。 |
| 自动测试与构建 | Java Surefire 104 项通过，失败、错误、跳过均为 `0`；Vue 类型、Prettier 与 1,737 模块生产构建通过；七个相关 PowerShell 脚本静态解析错误为 `0`。 |
| 桌面页面 | 1440×900 首页和客户任务页无横向溢出、破图或控制台 warning/error；任务表显示任务、项目、渠道、状态、更新时间和查看入口，已验收行无处理按钮。 |
| 移动页面 | 390×844 任务列表改为分组卡片，渠道、状态和更新时间无需横向拖动即可看到；页面宽度与内容宽度一致，破图和控制台 warning/error 均为 `0`。 |

本项只证明本机与隔离生产等价环境的技术闭环，不表示外部媒体到场、报道、渠道发布、供应商履约或真实生产已验收。相关外部数据、合同、法律信息、生产凭据、域名、TLS、监控和灾备继续保持待确认。

## 2026-07-30 P0 云采写与新闻发布会执行状态闭环

| 检查 | 结果 |
|---|---|
| 产品基准 | 产品负责人确认有效 PRD 合适并继续作为唯一验收基准。本项不增加组合服务，不改变云采写、媒体邀请、直编发稿、新闻发布会四项服务分别下单、分别形成项目、任务和订单的规则。 |
| 云采写状态 | 写手接单、提交初稿、客户退回、提交修订和客户确认均驱动客户任务与订单状态。待客户确认显示 `WAITING_CONFIRMATION`，退回后恢复 `IN_PROGRESS`，确认定稿后项目、任务和订单均进入完成终态。 |
| 退回与终态 | 客户退回稿件会重新开放原写手修订资格并恢复编辑任务；客户确认定稿后，运营不能继续提交新版本。已有版本正在客户审核时也不能重复提交，避免同一订单出现并行稿件版本。 |
| 服务边界 | 运营稿件编辑器只在独立云采写项目出现，不在媒体邀请、直编发稿或新闻发布会项目内出现。直编发稿的客户稿件入口保持原有独立规则，未将采写与媒体邀请重新合并。 |
| 发布会状态 | 九项发布会项目清单与发布会、项目和订单状态同步。任一清单开始后进入执行中；九项全部完成后，发布会、项目和客户订单同步完成；清单重新打开时按当前事实回到可执行状态。 |
| 真实接口闭环 | 本机自动化实际完成“写手接单—初稿—客户退回—修订—客户确认”，并验证审核中重复提交和确认后再提交均被拒绝；随后实际完成发布会第一项启动及九项全部完成，客户项目、任务与订单状态逐项一致。 |
| 权限与字段 | 三种角色登录、跨客户项目拒绝、客户任务与订单字段白名单、内部价格、供应商、成本、上游标识和令牌隔离继续通过。客户页面没有新增供应商或上游接口信息。 |
| 自动测试与构建 | Java Surefire 108 项通过，失败、错误、跳过均为 `0`；Vue 类型、Prettier 与生产构建通过，转换 1,737 个模块；本机完整 P0/P1 接口、数据库、角色权限与四服务记录回归全部通过。 |
| 运行与部署 | 本机四容器健康，健康接口返回数据库 `UP`、结构 `25`、合同 `winpress-v4.2.13-20260730`。隔离生产等价空库重新构建并启动成功，验证结束后容器、卷、镜像与临时凭据均自动清理。 |
| 跨端页面 | 客户订单页在 1440×900 保持完整表格；390×844 改为分组卡片，订单号、服务、项目、内容、金额或核验状态、状态、更新时间和查看入口无需横向拖动即可读取。两个视口均无页面级横向溢出、破图或控制台 warning/error。 |

本节证明四项独立服务中的云采写和新闻发布会已在本机与隔离部署条件下形成可验证的状态闭环。外部媒体与记者数据、供应商真实联动和履约、法律信息、正式生产账号与凭据、域名、TLS、监控及正式灾备仍未因此获得验收，继续明确为待确认。

## 2026-07-30 P0 直编发稿价格完整性

| 检查 | 结果 |
|---|---|
| 产品基准 | 有效 PRD 保持冻结。本项只保护直编发稿的客户价格快照、后台调价和订单计价，不改变四项服务边界，也不把供应商、成本或上游接口信息带到客户页面。 |
| 风险定位 | 原结构允许同一渠道并发生成多条 `ACTIVE` 报价；首次报价没有现成报价行可锁，批量调价又可能按不同输入顺序加锁。这会造成客户价格来源不唯一，并存在并发死锁风险。 |
| 数据库约束 | 新增迁移 `26-channel-quote-integrity.sql`。客户价必须大于零，成本价如存在则不得小于零或高于客户价，有效期结束时间必须晚于开始时间，报价状态只能使用约定枚举；每个渠道最多一条 `ACTIVE` 报价。约束均已验证，不改写既有价格。 |
| 并发控制 | 单条和批量调价先锁定渠道主记录，再读取和替换报价；批量渠道按稳定升序加锁。首次定价、重复调价和重叠批量调价因此使用同一串行化边界。 |
| 存量核验 | 执行迁移前复核 22,363 条有效报价和 420 条待复核报价：重复有效渠道、非正客户价、负成本、客户价低于成本及无效有效期均为 `0`。迁移重复执行通过，直接数据库回滚试验确认第二条有效报价被唯一约束拒绝且无测试残留。 |
| 自动测试与构建 | Java Surefire 108 项通过，失败、错误、跳过均为 `0`；Vue 类型、Prettier 与生产构建通过，转换 1,737 个模块。后台单条调价和批量调价锁定顺序已有自动测试。 |
| 数据与部署 | 干净数据库初始化、本机数据库备份恢复、生产 Compose 边界、生产等价空库冷启动及四项服务非空数据库与附件成对恢复全部通过。健康结构为 `26`，合同为 `winpress-v4.2.14-20260730`。 |
| 权限与页面 | 本机完整 P0/P1 回归通过三种角色、跨客户拒绝、四项服务订单稳定性和客户字段白名单。客户渠道页与订单页在 1280×720、390×844 实页复核无页面级横向溢出、破图或供应商、成本、上游标识、令牌泄漏。 |

本项证明价格记录在当前数据库和并发写入路径中具有唯一、可校验的来源，不表示目录中的媒体、报价时效或供应商履约已经获得外部授权。真实媒体可用性、供应商合同、正式价格授权及生产发布继续保持待确认。

## 2026-07-30 P0 媒体邀约执行闭环

| 检查 | 结果 |
|---|---|
| 产品基准 | 产品负责人确认现行有效 PRD 合适并冻结。本项只修复媒体邀约、任务、项目和订单之间的实现偏差，不改变四项服务独立下单、独立计价和客户可查规则。 |
| 风险定位 | 专用邀约记录已经保存“已邀请、已回复、婉拒”等事实，但客户任务投影隐藏这些阶段，通用任务、发布计划和项目也不会跟随更新。媒体婉拒后任务可能永久停留在待办；若强行完成，又会把没有报道成果的邀约误写成发布完成。 |
| 状态规则 | `INVITED`、`RESPONDED`、`ATTENDING` 驱动任务进入执行中；`DECLINED` 与 `NOT_PROCEEDING` 使用独立的 `NOT_PROCEEDING` 终态。该终态只允许 `MEDIA_PR` 使用，不能回退或补录发布成果。 |
| 客户可见性 | 客户任务显示邀约阶段、邀请时间和回复时间，但不显示沟通备注、运营人员、供应商、成本、外部记者标识或上游引用。客户订单显示“暂不推进”，不显示虚构金额或发布结果。 |
| 项目闭环 | 邀约状态变化与通用任务、发布计划和项目状态在同一事务内更新；计划确认后服务受理阶段关闭。全部候选终止时，项目和计划完成归档，但任务及订单仍明确标记“暂不推进”，不会被表述为媒体报道完成。 |
| 真实接口回归 | 本机演示环境实际执行合成的“确认名单—已邀请—已回复—媒体婉拒”链路，随后验证成果补录返回 `409 TASK_NOT_PROCEEDING`、任务退出待办、计划与项目闭合、订单为 `NOT_PROCEEDING`。该记录清楚标注为本机合成状态，不代表真实媒体联系。 |
| 数据库迁移 | 结构升级至 `27`，新增 `27-media-invitation-progress-integrity.sql`。迁移可重复执行，只对齐已有邀约事实；不生成联系、回复、到场、报道、价格、供应商或上游数据。约束校验、错位记录、伪造成果和挂起受理任务均为 `0`。 |
| 自动测试与构建 | Java Surefire 112 项通过，失败和错误均为 `0`；Vue 类型、Prettier 与生产构建通过，转换 1,737 个模块；PowerShell 脚本解析及三套 Compose 静态配置通过。 |
| 数据与部署 | 干净数据库初始化、本机数据库备份恢复、生产 Compose 边界、生产等价空库冷启动和四服务非空数据库/附件成对恢复均通过。健康接口返回结构 `27`、合同 `winpress-v4.2.15-20260730`。 |
| 跨端页面 | 客户任务页在 1280×720 显示完整表格，在 390×844 显示分组卡片；“暂不推进”和“邀约：媒体婉拒”均可见。客户订单页移动端可读取对应状态。两个视口均无页面级横向溢出、破图、控制台错误或内部字段文本。 |

本节证明媒体邀约在当前源码、本机演示数据和隔离生产等价环境中的状态闭环。外部媒体与记者目录授权、真实邀约、媒体回复、到场和报道、供应商合同与履约、法律信息、生产身份与凭据、域名、TLS、监控及正式灾备仍须独立验收。

## 2026-07-30 P0 发布任务终态旁路保护

| 检查 | 结果 |
|---|---|
| 产品基准 | 有效 PRD 继续作为唯一验收基准。本项未改变四项服务边界、发布会三项必填、客户可查闭环或客户与内部数据边界。 |
| 风险定位 | 供应商订单状态同步此前可绕过发布任务的常规终态保护，把 `COMPLETED`、`CLIENT_ACCEPTED` 或 `NOT_PROCEEDING` 任务重新改为执行中或异常。 |
| 应用控制 | 供应商订单同步更新关联发布任务时增加终态条件；客户已验收、已完成和暂不推进任务不再接受旁路更新。 |
| 数据库控制 | 结构升级至 `28`，新增 `28-publish-task-terminal-integrity.sql` 和触发器 `trg_publish_task_terminal_integrity`。客户已验收与暂不推进不可变；已完成只可保持完成或在存在已核验成果时进入客户已验收。 |
| 迁移验证 | 迁移在当前本机库重复执行通过；数据库事务内直接尝试 `COMPLETED → IN_PROGRESS` 被触发器拒绝，测试事务完整回滚。健康检查同时核对触发器存在。 |
| 自动测试与构建 | Java Surefire 112 项通过，失败、错误和跳过均为 `0`；Vue 类型、Prettier 和生产构建通过，转换 1,737 个模块；改动的 PowerShell 脚本静态解析错误为 `0`。 |
| 数据与部署 | 本机完整 P0/P1、干净数据库初始化、本机数据库与附件恢复、生产 Compose 边界、生产等价空库冷启动及四服务非空数据库/附件成对恢复全部通过。健康接口返回结构 `28`、合同 `winpress-v4.2.16-20260730`。 |
| 生产文案 | 默认生产构建中测试账号、测试身份选择及“本机演示环境”匹配均为 `0`。隐私页只说明尚未生效的政策及待验收的生产安全措施，不把本机测试环境写入客户文案。 |
| 跨端页面 | 客户任务页在 1280×720 与 390×844 下无页面级横向溢出、破图、控制台错误或内部字段文本；移动端项目入口与操作按钮达到 44px 触控高度。 |

本项没有启动或修改真实生产环境。外部媒体与记者数据、供应商合同及真实履约、法律信息、正式生产身份和凭据、域名、TLS、监控及正式灾备仍须独立验收。

## 2026-07-30 P0 发布计划保存幂等

| 检查 | 结果 |
|---|---|
| 产品基准 | 产品负责人确认现行有效 PRD 合适并冻结。本项只修复计划保存链路，不改变四项独立服务、客户可查闭环或客户与内部数据边界。 |
| 风险定位 | 同一页面的按钮锁不能覆盖超时重试、刷新续提和并发请求。旧接口可能保存两份相同计划，两份计划分别确认后会继续形成重复任务、内部订单与价格快照。 |
| 请求约束 | 复数和兼容单数计划保存路径均要求 16—80 位 `Idempotency-Key`。相同项目、创建人、标识和内容返回原计划；相同标识不能改作另一份计划。 |
| 前端续提 | 会话存储只保存项目/计划请求指纹和随机标识，不保存稿件或计划正文；同一内容在失败后复用标识，成功后清理，内容变化后生成新标识。 |
| 数据库迁移 | 结构升级至 `29`，新增 `29-publish-plan-idempotency.sql`。请求标识与摘要成对出现，同一项目和创建人的非空标识唯一；历史计划保持空值。 |
| 迁移实测 | 当前本机库连续执行迁移两次通过；数据库直接拒绝不成对字段和重复标识。测试事务回滚，测试计划残留为 `0`。 |
| 真实接口回归 | 缺少请求标识返回 `400`；同一标识与内容连续保存返回同一 `planNo`；同一标识与不同内容返回 `409`。直编与媒体邀请计划确认、任务生成、客户验收和媒体不再推进终态继续通过。 |
| 四服务闭环 | 现场采写、媒体邀请、直编发稿和新闻发布会均再次实测独立下单、项目、任务和订单记录；活动关联不合并价格、任务或订单。 |
| 自动测试与构建 | Java Surefire 114 项通过，失败、错误和跳过均为 `0`；Vue 类型和生产构建通过，转换 1,737 个模块；改动的 PowerShell 脚本解析错误为 `0`。 |
| 数据与部署 | 干净数据库初始化、本机数据库与附件恢复、生产 Compose 边界、生产等价空库冷启动和四服务非空数据库/附件成对恢复全部通过。健康接口返回结构 `29`、合同 `winpress-v4.2.17-20260730`。 |
| 跨端页面 | 直编发稿页在 1280×720 和 390×844 下无页面级横向溢出或破图，移动端可见操作目标不小于 44px；无 Vite 错误覆盖、显性异常或内部字段文本。 |

本节证明当前源码、本机数据和隔离生产等价环境中的发布计划防重，不代表静态媒体目录、报价时效、外部媒体授权或供应商履约已经验收。真实生产发布、法律信息、生产身份及凭据、域名、TLS、监控和正式灾备继续保持待确认。

## 2026-07-30 P0 结算交易登记幂等

| 检查 | 结果 |
|---|---|
| 产品基准 | 产品负责人确认现行有效 PRD 合适并冻结。本项只修复结算交易登记，不改变四项独立服务、客户订单、价格快照或内外数据边界。 |
| 风险定位 | 原接口只依赖页面提交锁，无法覆盖响应超时后的重试或并发请求。成功入账后再次提交可能重复改变实收、退款、调整和待结金额。 |
| 请求约束 | `POST /admin/settlements/{settlementId}/transactions` 要求 16—80 位 `Idempotency-Key`。同一结算、标识和内容返回原交易；同一标识不能改作另一笔交易。 |
| 前端续提 | 页面按结算与交易内容建立请求指纹，只在当前会话内保留随机请求标识；失败后复用，明确成功后清理，不保存凭据内容或内部备注。 |
| 数据库迁移 | 结构升级至 `30`，新增 `30-settlement-transaction-idempotency.sql`。请求标识与摘要成对出现，同一结算的非空标识唯一；历史交易不被改造成新的财务事实。 |
| 数据库实测 | 迁移连续执行通过；半套请求字段与重复标识均被数据库拒绝，事务回滚后无测试残留。结构列、成对约束、唯一索引和不匹配记录检查全部通过。 |
| 真实接口回归 | 缺少标识返回 `400`；相同标识和内容重试返回同一交易号；相同标识改传其他金额返回 `409`；最终仅一条 `CNY 12.34` 交易、待结金额为零，测试记录与会话随后清理。 |
| 自动测试与构建 | Java Surefire 116 项通过，失败、错误和跳过均为 `0`；Vue 类型、Prettier 与生产构建通过，转换 1,737 个模块；改动的 PowerShell 脚本解析通过。 |
| 数据与部署 | 本机完整 P0/P1、干净数据库、数据库与附件恢复、生产 Compose 边界、结构 `30` 生产等价空库冷启动和非空成对恢复全部通过。健康合同为 `winpress-v4.2.18-20260730`。 |
| 跨端页面 | 1280×720 内置浏览器和 390×844 隔离系统浏览器均实测平台结算页及交易弹窗。无页面级横向溢出、破图、Vite 覆盖或控制台错误；必填星号与标题同行，图标按钮具有可访问名称。 |

本节证明当前源码、本机数据库和隔离生产等价环境中的结算交易防重。真实媒体数据、供应商联动与履约、法律主体和政策、生产身份与凭据、域名、TLS、监控和正式灾备仍是上线前待确认门槛。

## 2026-07-30 P0 批量调价幂等

| 检查 | 结果 |
|---|---|
| 产品基准 | 产品负责人确认现行有效 PRD 合适并冻结。本项只修复平台批量调价，不改变四项独立服务、客户订单、价格快照或内外数据边界。 |
| 风险定位 | 原接口只依赖页面提交锁，无法覆盖响应超时后的重试。成功调整后的再次提交会在新价格上重复叠加百分比。 |
| 请求约束 | `POST /admin/channel-quotes/batch-adjust` 要求 16—80 位 `Idempotency-Key`。同一标识和内容返回原批次；同一标识不能改作另一批渠道、比例、有效期、对客说明或原因。 |
| 前端续提 | 页面按排序后的渠道、比例、UTC 有效期、对客说明和原因建立请求指纹；当前会话内失败续提复用随机标识，明确成功后清理。 |
| 数据库迁移 | 结构升级至 `31`，新增 `31-batch-quote-adjustment-idempotency.sql`。调价批次的标识、摘要、渠道数量和状态受约束；批量调价明细关联批次，历史单条调价保持不变。 |
| 数据库实测 | 迁移连续执行通过；重复标识、非法摘要、非法数量和非法状态均被数据库拒绝，事务回滚后隔离测试记录为 `0`。 |
| 真实接口回归 | 缺少标识返回 `400`；两条隔离渠道首次上调 10% 后为 `110`、`132`；同一标识重试返回同一批报价且价格不再变化；同一标识改传其他比例返回 `409`。最终一个完成批次、两条调价明细，随后全部清理。 |
| 自动测试与构建 | Java Surefire 119 项通过，失败、错误和跳过均为 `0`；Vue 类型、Prettier 与生产构建通过，转换 1,737 个模块；20 个 PowerShell 脚本静态解析错误为 `0`。 |
| 数据与部署 | 本机完整 P0/P1、干净数据库、数据库与附件恢复、生产 Compose 边界、结构 `31` 生产等价空库冷启动和四服务非空成对恢复全部通过。健康合同为 `winpress-v4.2.19-20260730`。 |
| 跨端页面 | 1280×720 与 390×844 实测平台调价页和批量调价弹窗。无页面级横向溢出、破图或控制台警告/错误；移动弹窗内部独立滚动，控件不超出视口，必填星号与标题同行。 |
| 测试数据 | 浏览器验收使用的临时平台账号、机构、角色和隔离调价数据均已删除；没有把测试密码、令牌或本机账号说明写入源码包。 |

本节证明当前源码、本机数据库和隔离生产等价环境中的批量调价防重。真实媒体目录、供应商联动和履约、法律主体与政策、生产身份和凭据、域名、TLS、监控及正式灾备仍是上线前待确认门槛。

## 2026-07-30 P0 直编发稿计划保存

| 检查 | 结果 |
|---|---|
| 产品基准 | 有效 PRD 保持冻结。本项只修复独立直编发稿项目的计划保存，不改变四项服务、客户字段边界或计划确认规则。 |
| 故障复现 | 页面把客户项目详情中不存在的内部 `id` 当作项目编号，请求发到 `/projects/undefined/publish-plans`，后端返回通用错误。 |
| 字段边界 | 没有把内部数据库编号补回客户项目详情；页面改用客户已从项目列表或路由选定的项目编号。 |
| 防错处理 | 保存和确认前验证项目编号为正安全整数；后端将数字路径参数格式错误稳定映射为 `400 INVALID_REQUEST_PARAMETER`。 |
| 页面实测 | 使用原项目与截图中的两家渠道完成一次真实保存，返回公开计划编号、两项及 `¥80.00` 合计；未确认计划、未生成任务，临时计划及明细已精确清理，残留为 `0`。 |
| 跨端体验 | 默认桌面视口与 `390×844` 移动端均能完整操作核对弹窗；页面无控制台 warning/error，后端日志未再出现 `undefined` 项目编号。 |
| 自动测试与构建 | Java Surefire `120` 项通过，失败、错误和跳过均为 `0`；Vue 类型、Prettier 和生产构建通过，转换 `1,737` 个模块。 |
| 数据与部署 | 本机完整 P0/P1 权限与四服务回归、结构 `31` 干净库初始化、健康检查及隔离生产空库冷启动通过；运行合同为 `winpress-v4.2.20-20260730`。 |

本节只证明当前源码、本机环境及隔离生产等价空库中的计划保存修复。外部媒体目录授权、真实报价与供应商履约、法律信息、生产身份和凭据、域名、TLS、监控与正式灾备仍须上线前单独验收。

## 2026-07-30 P1 部署迁移说明一致性

| 检查 | 结果 |
|---|---|
| 产品基准 | 有效 PRD 保持冻结。本项只修复部署说明和可执行迁移基线之间的偏差，不改变四项独立服务、客户权限、订单计价或内外数据边界。 |
| 风险定位 | 运行时已要求数据库结构 `31`，但部分现行文档仍指示执行到迁移 `30`、`22` 或把审查范围写到 `29`。存量库按旧说明升级会遗漏批量调价幂等表与约束，并在健康检查阶段失败。 |
| 文档修复 | README、数据库说明、迁移说明、部署说明、交接说明和全局审查的现行范围统一到 `31`；历史快照仍保留原时点语义，不作为当前部署指令。 |
| 自动防漂移 | `verify-production-compose-boundaries.ps1` 同时读取六份现行操作文档，核对本机初始化、存量升级、恢复范围和健康结构必须包含 `31`，并拒绝再次出现旧终点。 |
| 静态与构建 | 19 个 PowerShell 脚本在 Windows PowerShell 5.1 下静态解析错误为 `0`；Java Surefire `120` 项通过；Vue 类型、Prettier 和生产构建通过，转换 `1,737` 个模块。 |
| 接口与权限 | 本机完整 P0/P1 回归通过健康、三角色、跨客户隔离、四项服务任务与订单、客户字段白名单、成本/供应商后台隔离及未授权媒体不可用边界。 |
| 数据库 | 全新 PostgreSQL 按结构 `31` 完整初始化，工作流表和约束齐全；静态种子完整性为 `22,364` 个渠道、`22,363` 条报价，未据此宣称外部授权或真实可发布。 |
| 生产等价启动 | 生产 Compose 边界和隔离冷启动通过：结构 `31`、合同 `winpress-v4.2.20-20260730`、空业务数据、私有接口文档、明确 HTTPS 来源、受保护集成、`980 元/人天` 公开采写价及仅回环端口；临时容器、卷、镜像和凭据均清理。 |
| 跨端页面 | 1280×720 首页与直编发稿页、390×844 首页与渠道筛选页均无页面级横向溢出、破图或控制台 warning/error；移动端首屏可见交互目标均不小于 40px。 |

本节证明现行部署说明、数据库结构门槛和可执行验收脚本已经一致，不表示真实媒体数据、报价时效、供应商履约、法律信息、生产身份及凭据、域名、TLS、监控或正式灾备已经验收。

## 2026-07-31 P1 媒体候选状态接口数据边界

| 检查 | 结果 |
|---|---|
| 产品基准 | 有效 PRD 的客户信息边界不变：未获得书面授权、数据范围、令牌、限流策略和联调验收前，媒体与记者候选不可表述为已接通的实时能力。 |
| 风险定位 | 客户可调用的媒体筛选状态接口原先除功能开关外还返回了运行配置、治理就绪和验收状态。这些字段不含凭据，但会让客户侧推断内部接入进度，超出客户完成下单和人工补充所需的信息。 |
| 修复 | `NiumediaMediaService.status()` 只返回当前媒体、记者、筛选字典是否可用、通用降级状态和人工补充能力。运行配置、原始媒体/记者开关、治理就绪和验收状态移入仅 `PLATFORM_ADMIN` 可读取的 `adminStatus()`。 |
| 后台一致性 | 集成管理页使用受保护的管理员状态读取原始运行配置；客户接口不再返回 `runtimeConfigured`、`governanceReady`、`verificationStatus` 或原始检索配置字段。管理员侧不会因此把配置存在误表述为外部数据已经验收。 |
| 降级行为 | 当前本机未验收外部媒体资料时，客户状态为不可用并保留人工补充入口；保存候选仍进入项目核验，不自动邀请、不创建外部供应商履约、不记录媒体到场或报道。 |
| 自动测试 | 媒体服务与集成管理定向 Java 测试、完整 Java Surefire 测试、Vue 类型和格式检查、Vite 生产构建均通过。新增回归断言覆盖客户状态字段白名单与管理员专属状态。 |
| 实际接口回归 | 本机 P0/P1 角色回归通过：客户媒体状态不含内部配置和验收字段，未授权检索保持不可用且可人工补充；客户和运营均无法访问后台集成配置，管理员接口保持受保护可用。 |
| 跨端页面 | 媒体邀请页桌面和移动 E2E 回归 `2/2` 通过：在线筛选不可用提示、人工补充入口、媒体先选后记者的交互边界以及无平行筛选卡片均保持正确。 |
| 运行与部署 | 本机端口预检通过后，前端、后端、PostgreSQL 和 Redis 容器均健康。健康接口为 `UP`，数据库 `UP`，结构 `40`，接口合同 `winpress-v4.2.29-20260731`。本项无数据库迁移。 |

本节只证明当前源码和本机演示部署中的客户字段边界与安全降级。外部媒体或记者数据的授权、数据范围、令牌、限流、沙箱、生产联调和实际使用许可仍须由权利方提供书面证据并单独验收，不能由本机回归替代。

## 2026-07-31 P0 发布会候选状态与时间线完整性

| 检查 | 结果 |
|---|---|
| 产品基准 | 四项服务独立下单、独立项目、任务和订单的有效 PRD 不变。发布会拟邀名单只是项目协同对象，不等于媒体已经接受、到场、采访或报道。 |
| 风险定位 | 候选更新入口原先没有并发状态校验，运营人员可跳过待邀约确认和已邀请阶段，直接将候选写为到场；陈旧页面也可能覆盖更晚的进度。 |
| 服务修复 | API 增加 `expectedStatus`，服务端在行锁内比较当前状态；非法跳级返回 `409 INVALID_CONFERENCE_MEDIA_CANDIDATE_TRANSITION`，陈旧提交返回 `409 CONFERENCE_MEDIA_CANDIDATE_STATE_CHANGED`，终态回退返回 `409 CONFERENCE_MEDIA_CANDIDATE_FINALIZED`。 |
| 数据库迁移 | 新增迁移 `41-conference-media-candidate-state-integrity.sql`，建立状态／时间线／结果说明约束和状态触发器；不补写任何邀请、回复、到场、联系人或履约事实。迁移前本机备份的 SHA-256 为 `9AA45574BE920FC2EE6DE7D3FED5C56D0ADC88403723A636E0968AFF3CC27C22`。 |
| 页面修复 | 运营页面仅展示当前状态允许的下一步；到场、婉拒和暂不推进后移除状态选择与保存按钮。候选负责人和时间线改为可读的高对比颜色。 |
| 自动回归 | Java 完整测试、Vue 类型与格式检查、Vite 构建、PowerShell 脚本解析均通过。本机 P0/P1 真实接口回归验证候选不能直跳到场、不能从终态回退，客户投影不含内部标识或跟进记录。 |
| 数据库与部署 | 干净本机库、冷启动守卫、数据库备份恢复、生产 Compose 边界和隔离生产等价空库冷启动均通过；运行健康为结构 `41`、合同 `winpress-v4.2.30-20260731`。生产等价结果不等于实际生产部署。 |
| 跨端页面 | Playwright 桌面 `1440×900` 与移动 `390×844` 回归通过：发布会三项必填、媒体名单终态锁定和未验收媒体资料的人工补充路径均无横向溢出；候选页通过 Axe 严重／关键级无障碍扫描。 |

详见 [`CONFERENCE-MEDIA-CANDIDATE-STATE-INTEGRITY-QA-20260731.md`](CONFERENCE-MEDIA-CANDIDATE-STATE-INTEGRITY-QA-20260731.md)。本节不表示外部媒体数据、真实媒体邀请、供应商履约、法律主体、域名 TLS、监控或正式灾备已经验收。
