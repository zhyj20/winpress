# 本机 P0/P1 闭环回归记录（2026-07-31）

## 范围与边界

本记录只对应 `docker-compose.local-demo.yml` 的本机演示环境，不构成生产上线、外部数据授权或供应商履约验收。

- 产品边界：云采写、媒体邀请、直编发稿、新闻发布会均为独立服务；不得新建组合服务 `WRITING_AND_PUBLISHING`。
- 客户边界：客户只可查看本组织的项目、任务、订单和交付信息；供应商、成本、上游接口、令牌及内部规则不对客户展示。
- 外部边界：牛媒及其他媒体数据、供应商真实履约在取得授权、合同和联调证据前，维持“暂不可用”或人工补充路径，不作为实时能力宣传。

## 本轮改动

1. 补充 `docs/TEST-ACCOUNTS.md` 的适用范围，明确测试账号仅用于本机演示编排，生产环境不得加载测试账号、案例或默认密码。
2. 明确“品牌案例演示账号”表仅用于说明案例覆盖范围，不能被误读为新增登录账号清单。
3. 将 22 份历史传播汇报按“活动传播”和“直编发稿”归集到受控展示页；页面不提供原始文件下载、不公开本机路径、联系人、报价或原始媒体名单。

## 数据保护与回滚边界

- 未执行数据库结构迁移、自动拆单或删除历史组合服务记录。
- 在本机测试账号同步前，已创建 PostgreSQL 备份：
  `artifacts/backups/winpress-local-before-test-account-sync-20260731-155217.dump`
- 备份 SHA-256：
  `D66934BC14866480A20E016DF3062058DC11E3474DC08D5760B0B483B76EBEB5`
- 回滚前必须先确认该备份后没有需要保留的本机演示数据；不得以“回滚”为由覆盖后续业务数据。

## 已执行验证

| 项目 | 命令或方式 | 结果 |
| --- | --- | --- |
| 后端单元与集成测试 | `backend\\mvnw.cmd test` | 169 个测试通过，0 failure / 0 error |
| 前端静态检查 | `npm.cmd run check` | 通过 |
| 前端生产构建 | `npm.cmd run build` | 通过 |
| 前端端到端回归 | 按 `desktop`、`tablet`、`mobile` 串行执行 Playwright | `108/108` 通过；详见 [`CROSS-VIEWPORT-E2E-QA-20260801.md`](CROSS-VIEWPORT-E2E-QA-20260801.md) |
| 本机 P0/P1 权限与数据边界 | `scripts/verify-local-p0p1.ps1` | 通过 |
| 四服务业务闭环 | `scripts/verify-local-p0p1.ps1 -CreateActivityClosure` | 通过；仅创建明确标记的本机 QA 活动数据 |
| 本机容器健康 | Docker Compose + `/api/v1/health` | 前端、后端、PostgreSQL、Redis 均正常；数据库 schema 版本 41 |
| 历史汇报页视觉检查 | 1440px 与 390px 视口 | 无控制台错误、无横向溢出；分类筛选生效 |

## 闭环覆盖

- 云采写：任务分配、报价确认、初稿、退回修改、客户确认、项目/任务/订单关闭。
- 新闻发布会：仅三项首要资料可提交，九项活动清单、任务推进、项目/订单关闭。
- 媒体邀请：媒体上下文优先、候选记者、人工邀请与回应记录；不虚构媒体报道。
- 直编发稿：稿件确认、计划保存、供应商控制记录、履约凭据、客户确认与订单关闭。
- 权限：客户跨组织数据不可读取；运营与管理员权限分离；管理员集成状态不回显密钥值。

## 2026-08-01 复跑证据

- 本次未执行结构迁移、历史组合服务处理或任何生产操作；运行前已对当前本机演示库创建恢复点：
  `artifacts/database-backups/winpress-pre-local-closure-20260801-210742.dump`。
- 该恢复点的 SHA-256 为
  `7A4DCF95333E408E95FCC58126F24FD6BD1A2C033BACBBE8E2F9539FF31F96B2`。
  回退前必须确认其中没有需要保留的后续本机演示事实，禁止以回退名义覆盖业务数据。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/verify-local-p0p1.ps1 -CreateActivityClosure`
  以退出码 `0` 完成。该脚本在 `docker-compose.local-demo.yml` 环境中新增带 `Local QA` 标记的测试活动，验证四项独立服务的项目、任务与订单映射，媒体邀请人工路径、直编发稿供应商履约凭据门禁、客户验收、跨客户拒绝及客户/运营/管理员字段与权限边界。
- 运行时公共健康检查为应用、数据库和结构均 `UP`；迁移 `41` 与接口合同
  `winpress-v4.2.30-20260731` 仅在受控源码、数据库台账和构建证据中核验，公共响应不回显这些部署元数据；前端、后端、PostgreSQL 与 Redis 容器均为 `healthy`。
- 三视口浏览器回归已分别取得最终 `passed` 状态：在本轮结算状态机和移动结算卡修补后，桌面、平板、移动各 37 项，合计 `111/111`；覆盖角色菜单、服务入口、订单/任务记录、供应商履约管理、客户字段投影、关键深链筛选、结算状态直接转换和移动端无内部横向滚动。该证据只适用于当前本机 Docker 演示栈。
- 这次测试生成的数据只用于本机回归，不能解释为真实客户订单、真实媒体联系、真实供应商受理或生产履约。

## 2026-08-01 恢复与冷启动边界

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| 本机数据库备份恢复 | 通过；53 张业务表、四项独立服务类型、关键汇总、迁移台账与开放 API 治理结构一致 | 仅当前 `docker-compose.local-demo.yml` 数据库；不含上传文件，不证明生产恢复时限。 |
| 本机上传卷恢复 | 通过；28 个文件、哈希、所有权、权限与大小一致 | 仅当前本机演示上传卷；不暴露文件名或内容。 |
| 干净本机数据库冷启动 | 通过；本机受控目录输入为 22,364 条渠道和 22,363 条报价，四项服务结构存在 | 只证明本机受控静态输入可初始化；不证明任何外部数据授权、实时检索或价格时效。 |
| 生产 Compose 静态边界 | 通过；未自动加载测试账号、演示订单或未验收媒体目录 | 未启动真实生产环境。 |

同日安全与金额回归：

- `scripts/verify-local-session-revocation.ps1` 通过：账号状态已恢复、缺失会话索引仍可撤销、恢复后旧令牌被拒绝、新登录可用。
- `scripts/verify-local-settlement-idempotency.ps1` 通过：缺少幂等键被拒绝；同一收款请求只保留一笔账务事实；同一键改写金额被拒绝；脚本在结束时删除临时结算与审计记录。
- `scripts/verify-local-batch-quote-idempotency.ps1` 通过：缺少幂等键被拒绝；同一批量调价重试返回原结果；同一键改写比例被拒绝；脚本在结束时删除临时渠道、报价与调价台账。
- 未配置的 GEO 服务端联动端点 `POST /api/v1/integrations/geo/orders`、`/quotes` 及 `/catalog/direct-publishing-offers` 均返回 `503 FEDERATION_UNAVAILABLE`；未进入订单、报价或目录实际处理。

### 公共健康信息收口

- **问题与根因：** 匿名健康路由被 Docker 健康检查使用，但历史实现同时返回结构版本、接口合同和构建元数据；这些字段不是探针可用性所需，且与“公开响应不泄露部署内部信息”的边界不一致。
- **修复：** `/api/v1/health` 现仅返回 `status`、`database`、`schemaStatus`。精确迁移 `41` 继续由同一后端结构就绪查询、受控数据库 `schema_migration_ledger`、源码构建和发布证据共同验证；部署文档与本机/生产演练脚本已同步，不再将公开字段当作版本门禁。
- **验证：** 先行失败测试确认旧响应仍含字段；修复后 `HealthControllerTest`、完整 Java `172/172`、Java 打包、Vue 类型/格式/生产构建、`verify-local-p0p1.ps1`、隔离数据库/上传恢复、干净本机数据库和生产 Compose 静态边界检查均通过。运行态匿名响应为 HTTP `200`，仅含三项状态且均为 `UP`。
- **回滚边界：** 本项未修改数据库结构、业务数据、服务状态、订单或外部接口。若回退，只回退健康响应字段、相应测试、脚本和文档；不得借回退动作覆盖本机业务数据或把内部版本字段重新公开。

### 结算状态与运行产物一致性补充

- 人工结算状态更新现在采用显式直接转换：`PENDING → CONFIRMED/CANCELLED`、`CONFIRMED → PAID/CANCELLED`；已结清和已取消结算单不能被人工状态更新重新开启。非法回退返回 `409 / INVALID_SETTLEMENT_TRANSITION`，交易、退款及审计记录仍由既有账务路径保留。
- 管理员结算页只显示当前结算单可直接进入的状态；移动端把宽表转换为完整信息卡，避免横向滚动隐藏客户／项目和状态操作。
- 发现本机 Docker 后端依赖 `backend/target-local` 预制 JAR 后，新增 `scripts/verify-local-demo-runtime-artifact.ps1` 并纳入统一重建入口。它会强制比较主机 JAR 和容器 `/app/app.jar` 的 SHA-256，不一致即失败。本轮重建后哈希一致，四个容器均 `healthy`。
- 本项未执行结构迁移、自动拆单、历史组合服务处理或业务数据写入；运行时非法状态回退测试被后端拒绝，不改变结算单。
- 统一重建后的 `scripts/verify-local-p0p1.ps1`（未带 `-CreateActivityClosure`）再次以退出码 `0` 完成：认证、客户隔离、四项独立服务账本、历史组合归档、管理员内部字段边界、外部数据不可用降级和供应商接口待验收门禁均通过。本次默认回归只发送授权与拒绝路径检查，不创建活动闭环数据。

生产等价冷启动脚本曾在隔离容器健康等待阶段被当前自动化运行器中断；隔离镜像、临时容器、卷、网络、临时凭据和状态文件均已通过脚本的 `-CleanupOnly` 路径清理。由于未获得脚本最终“通过”输出，本记录**不把生产等价冷启动列为已验收**。应在可持续运行的预发布执行器中重新运行 `scripts/verify-production-cold-start.ps1`，并保留完整退出码与非敏感日志作为生产门禁证据。

## 尚未满足的生产上线条件

以下项目未在本机演示环境中验证，必须在隔离预发布环境经主体方与外部合作方确认后完成：

1. 外部媒体数据的正式授权、字段范围、令牌、限流和异常联调。
2. 供应商真实下单、报价、回执、对账、重试和服务等级验收。
3. 主体、备案、联系方式、法律文本、案例授权及生产凭据核验。
4. 生产冷启动、备份恢复、最小权限网络、TLS/域名切换、监控告警和生产迁移演练。
5. 存量 `WRITING_AND_PUBLISHING` 记录的业务确认、映射或归档；禁止自动拆分或删除。

## 本机访问地址

- 首页：`http://127.0.0.1:5217/`
- 案例：`http://127.0.0.1:5217/cases`
- 历史传播汇报归集：`http://127.0.0.1:5217/case-evidence/historical-reports`
- 健康检查：`http://127.0.0.1:8192/api/v1/health`
